\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm,mathtools}
\usepackage{bm}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{booktabs}
\usepackage{array}
\usepackage{setspace}
\usepackage{mathrsfs}
\usepackage{physics}

\geometry{margin=1in}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=blue, citecolor=black}
\setlist[enumerate]{topsep=0.4em,itemsep=0.35em}
\setlist[itemize]{topsep=0.3em,itemsep=0.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}
\setstretch{1.06}

\newcommand{\Aether}{\AE{}ther}
\newcommand{\Aflow}{\AE{}ther-flow}
\newcommand{\TheoryName}{\Aflow{} Interpretation of Relativity}
\newcommand{\TheoryProgram}{\Aether{} / \Aflow{} framework}
\newcommand{\Sub}{\mathcal A}
\newcommand{\ObserverMap}{\Xi}
\newcommand{\ObserverMetric}{g^{\mathrm{br}}}
\newcommand{\CandidateMetric}{g^{\mathrm{cand}}}
\newcommand{\CompletedMetric}{g^{\sharp}}
\newcommand{\BaseShift}{\Sigma^{\mathrm{IER}}}
\newcommand{\ResponseShift}{\Sigma^{\mathrm{resp}}}
\newcommand{\CompShift}{\Sigma^{\mathrm{cmp}}}
\newcommand{\BaseLift}{\widehat\Sigma^{\mathrm{IER}}}
\newcommand{\ResponseLift}{\widehat\Sigma^{\mathrm{resp}}}
\newcommand{\CompLift}{\widehat\Sigma^{\mathrm{cmp}}}
\newcommand{\ResidualCore}{\mathcal V_{\mu\nu}^{\mathrm{res}}}
\newcommand{\ResidualLift}{\widehat{\mathcal V}^{\mathrm{res}}}
\newcommand{\SubReducedEin}{\widehat{\mathcal L}}
\newcommand{\FreeProj}{\Pi_{\mathrm{free}}}
\newcommand{\GaugeAux}{Y}
\newcommand{\ReducedEin}{\mathcal L_{\ObserverMetric}}
\newcommand{\CompClass}{\mathscr C_{\mathrm{cmp}}}
\newcommand{\MicFunctional}{\mathscr F_{\mathrm{mic}}}
\newcommand{\PrimFunctional}{\mathscr F_{\mathrm{prim}}}
\newcommand{\CarrierFunctional}{\mathscr F_{\mathrm{car}}}
\newcommand{\DeepFunctional}{\mathscr F_{\mathrm{deep}}}
\newcommand{\StemFunctional}{\mathscr F_{\mathrm{sel}}}
\newcommand{\PrimStrain}{K}
\newcommand{\PrimNeutral}{J}
\newcommand{\PrimFree}{F}
\newcommand{\CarrierSeed}{\mathfrak S}
\newcommand{\RelabelSeed}{\mathfrak R}
\newcommand{\FreeSeed}{\mathfrak F}
\newcommand{\LieU}{\mathfrak u}

\newcommand{\SectorSet}{\mathsf S}
\newcommand{\Car}{\mathrm{car}}
\newcommand{\Cur}{\mathrm{cur}}
\newcommand{\Hom}{\mathrm{hom}}

\newcommand{\ObjClass}[1]{\mathcal O_{#1}}
\newcommand{\StatTagClass}[1]{\mathcal Y_{#1}^{\mathrm{st}}}
\newcommand{\DensityClass}[1]{\mathscr N_{#1}}
\newcommand{\FluxClass}[1]{\mathscr J_{#1}}
\newcommand{\PhaseReadClass}[1]{\mathscr R_{#1}}
\newcommand{\RootClass}[1]{\mathscr U_{#1}}

\newcommand{\ConfigSpace}[1]{\mathcal Q_{#1}}
\newcommand{\ShellTarget}[1]{\mathcal E_{#1}}
\newcommand{\ShellMetric}[1]{m_{#1}^{\mathrm{sh}}}
\newcommand{\ShellGap}[1]{\eta_{#1}}
\newcommand{\ConstraintTarget}[1]{\mathcal K_{#1}}
\newcommand{\ConstraintMetric}[1]{m_{#1}^{\mathrm{con}}}
\newcommand{\ConstraintGap}[1]{\delta_{#1}}
\newcommand{\OriginTarget}[1]{\mathcal H_{#1}^{\mathrm{orig}}}
\newcommand{\OriginTargetMetric}[1]{n_{#1}^{\mathrm{orig}}}
\newcommand{\OriginGap}[1]{\epsilon_{#1}^{\mathrm{orig}}}
\newcommand{\RootTarget}[1]{\mathcal Z_{#1}^{\mathrm{root}}}
\newcommand{\RootTargetMetric}[1]{p_{#1}^{\mathrm{root}}}
\newcommand{\RootGap}[1]{\kappa_{#1}^{\mathrm{root}}}
\newcommand{\PrimTarget}[1]{\mathcal W_{#1}^{\mathrm{prim}}}
\newcommand{\PrimTargetMetric}[1]{q_{#1}^{\mathrm{prim}}}
\newcommand{\PrimGap}[1]{\lambda_{#1}^{\mathrm{prim}}}
\newcommand{\SubprimTarget}[1]{\mathcal V_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimTargetMetric}[1]{v_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimGap}[1]{\mu_{#1}^{\mathrm{subprim}}}
\newcommand{\ProtoSubprimTarget}[1]{\mathcal T_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimTargetMetric}[1]{t_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimGap}[1]{\nu_{#1}^{\mathrm{psp}}}
\newcommand{\PreProtoSubprimTarget}[1]{\mathcal X_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimTargetMetric}[1]{x_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimGap}[1]{\omega_{#1}^{\mathrm{ppsp}}}

\newcommand{\PrimSpace}[1]{\mathcal P_{#1}}
\newcommand{\PrimRead}[1]{\Pi_{#1}^{\mathrm{prim}}}
\newcommand{\PrimUnit}[1]{u_{#1}^{\mathrm{prim}}}
\newcommand{\PrimTimeRev}[1]{\sigma_{#1}^{\mathrm{prim}}}
\newcommand{\PrimMetric}[1]{\breve M_{#1}}
\newcommand{\PrimSection}[1]{\mathfrak P_{#1}^{\mathrm{prim}}}
\newcommand{\PrimInc}[1]{\iota_{#1}^{\mathrm{prim}}}
\newcommand{\PrimLiftedPreAmbient}[1]{\widehat{\mathfrak Z}_{#1}^{\mathrm{root}}}
\newcommand{\PrimLiftedAmbient}[1]{\widehat{\mathfrak S}_{#1}^{\mathrm{orig}}}
\newcommand{\PrimLiftedShell}[1]{\widehat{\mathfrak D}_{#1}^{\mathrm{root}}}
\newcommand{\PrimConstraintMap}[1]{\widehat{\mathfrak C}_{#1}^{\mathrm{root}}}
\newcommand{\PrimCotangentRealization}[1]{\breve\Psi_{#1}}
\newcommand{\PrimLiouville}[1]{\boldsymbol{\Lambda}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimTwo}[1]{\boldsymbol{\Xi}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimConnectionForm}[1]{\mathcal C_{#1}^{\mathrm{conn}}}
\newcommand{\PrimTransport}[1]{\mathbb U_{#1}}
\newcommand{\PrimShadowForm}[1]{\boldsymbol{\varsigma}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimRateForm}[1]{\boldsymbol{\lambda}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimDensitySource}[1]{\boldsymbol{q}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimFluxSource}[1]{\boldsymbol{j}_{#1}^{\mathrm{prim}}}
\newcommand{\PrimBoundarySource}[1]{\boldsymbol{\rho}_{#1}^{\partial,\mathrm{prim}}}
\newcommand{\PrimAction}[1]{\mathscr A_{#1}^{\mathrm{prim}}}

\newcommand{\SubprimSpace}[1]{\mathcal G_{#1}}
\newcommand{\SubprimRead}[1]{\Pi_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimUnit}[1]{u_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimTimeRev}[1]{\upsilon_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimMetric}[1]{M_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimSection}[1]{\mathfrak G_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimInc}[1]{\iota_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimLiftedPrimitive}[1]{\overline{\mathfrak G}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimLiftedRoot}[1]{\overline{\mathfrak P}_{#1}^{\mathrm{prim}}}
\newcommand{\SubprimLiftedPreAmbient}[1]{\overline{\mathfrak Z}_{#1}^{\mathrm{root}}}
\newcommand{\SubprimLiftedAmbient}[1]{\overline{\mathfrak S}_{#1}^{\mathrm{orig}}}
\newcommand{\SubprimLiftedShell}[1]{\overline{\mathfrak D}_{#1}^{\mathrm{root}}}
\newcommand{\SubprimConstraintMap}[1]{\overline{\mathfrak C}_{#1}^{\mathrm{root}}}
\newcommand{\SubprimCotangentRealization}[1]{\overline{\Psi}_{#1}}
\newcommand{\SubprimLiouville}[1]{\boldsymbol{\Lambda}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimTwo}[1]{\boldsymbol{\Xi}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimConnectionForm}[1]{\mathcal D_{#1}^{\mathrm{conn}}}
\newcommand{\SubprimTransport}[1]{\mathbb V_{#1}}
\newcommand{\SubprimShadowForm}[1]{\boldsymbol{\varsigma}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimRateForm}[1]{\boldsymbol{\lambda}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimDensitySource}[1]{\boldsymbol{q}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimFluxSource}[1]{\boldsymbol{j}_{#1}^{\mathrm{subprim}}}
\newcommand{\SubprimBoundarySource}[1]{\boldsymbol{\rho}_{#1}^{\partial,\mathrm{subprim}}}
\newcommand{\SubprimAction}[1]{\mathscr A_{#1}^{\mathrm{subprim}}}

\newcommand{\ProtoSubprimSpace}[1]{\mathcal N_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimRead}[1]{\Pi_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimUnit}[1]{u_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimTimeRev}[1]{\vartheta_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimMetric}[1]{N_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimSection}[1]{\mathfrak T_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimInc}[1]{\jmath_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimLiftedPrimitive}[1]{\check{\mathfrak G}_{#1}^{\mathrm{subprim}}}
\newcommand{\ProtoSubprimLiftedRoot}[1]{\check{\mathfrak P}_{#1}^{\mathrm{prim}}}
\newcommand{\ProtoSubprimLiftedPreAmbient}[1]{\check{\mathfrak Z}_{#1}^{\mathrm{root}}}
\newcommand{\ProtoSubprimLiftedAmbient}[1]{\check{\mathfrak S}_{#1}^{\mathrm{orig}}}
\newcommand{\ProtoSubprimLiftedShell}[1]{\check{\mathfrak D}_{#1}^{\mathrm{root}}}
\newcommand{\ProtoSubprimConstraintMap}[1]{\check{\mathfrak C}_{#1}^{\mathrm{root}}}
\newcommand{\ProtoSubprimCotangentRealization}[1]{\check{\Psi}_{#1}}
\newcommand{\ProtoSubprimLiouville}[1]{\boldsymbol{\Lambda}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimTwo}[1]{\boldsymbol{\Xi}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimConnectionForm}[1]{\mathcal E_{#1}^{\mathrm{conn}}}
\newcommand{\ProtoSubprimTransport}[1]{\mathbb W_{#1}}
\newcommand{\ProtoSubprimShadowForm}[1]{\boldsymbol{\varsigma}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimRateForm}[1]{\boldsymbol{\lambda}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimDensitySource}[1]{\boldsymbol{q}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimFluxSource}[1]{\boldsymbol{j}_{#1}^{\mathrm{psp}}}
\newcommand{\ProtoSubprimBoundarySource}[1]{\boldsymbol{\rho}_{#1}^{\partial,\mathrm{psp}}}
\newcommand{\ProtoSubprimAction}[1]{\mathscr A_{#1}^{\mathrm{psp}}}

\newcommand{\PreProtoSubprimSpace}[1]{\mathcal B_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimRead}[1]{\Pi_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimUnit}[1]{u_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimTimeRev}[1]{\chi_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimMetric}[1]{B_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimSection}[1]{\mathfrak U_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimInc}[1]{\iota_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimLiftedSubprimitive}[1]{\widetilde{\mathfrak T}_{#1}^{\mathrm{psp}}}
\newcommand{\PreProtoSubprimLiftedPrimitive}[1]{\widetilde{\mathfrak G}_{#1}^{\mathrm{subprim}}}
\newcommand{\PreProtoSubprimLiftedRoot}[1]{\widetilde{\mathfrak P}_{#1}^{\mathrm{prim}}}
\newcommand{\PreProtoSubprimLiftedPreAmbient}[1]{\widetilde{\mathfrak Z}_{#1}^{\mathrm{root}}}
\newcommand{\PreProtoSubprimLiftedAmbient}[1]{\widetilde{\mathfrak S}_{#1}^{\mathrm{orig}}}
\newcommand{\PreProtoSubprimLiftedShell}[1]{\widetilde{\mathfrak D}_{#1}^{\mathrm{root}}}
\newcommand{\PreProtoSubprimConstraintMap}[1]{\widetilde{\mathfrak C}_{#1}^{\mathrm{root}}}
\newcommand{\PreProtoSubprimCotangentRealization}[1]{\widetilde{\Psi}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimLiouville}[1]{\boldsymbol{\Lambda}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimTwo}[1]{\boldsymbol{\Xi}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimConnectionForm}[1]{\mathcal F_{#1}^{\mathrm{conn}}}
\newcommand{\PreProtoSubprimTransport}[1]{\mathbb X_{#1}}
\newcommand{\PreProtoSubprimShadowForm}[1]{\boldsymbol{\varsigma}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimRateForm}[1]{\boldsymbol{\lambda}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimDensitySource}[1]{\boldsymbol{q}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimFluxSource}[1]{\boldsymbol{j}_{#1}^{\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimBoundarySource}[1]{\boldsymbol{\rho}_{#1}^{\partial,\mathrm{ppsp}}}
\newcommand{\PreProtoSubprimAction}[1]{\mathscr A_{#1}^{\mathrm{ppsp}}}

\newcommand{\SelSpace}[1]{\mathcal M_{#1}^{\mathrm{sel}}}
\newcommand{\SelRead}[1]{\Pi_{#1}^{\mathrm{sel}}}
\newcommand{\SelUnit}[1]{u_{#1}^{\mathrm{sel}}}
\newcommand{\SelTimeRev}[1]{\tau_{#1}^{\mathrm{sel}}}
\newcommand{\SelMetric}[1]{S_{#1}^{\mathrm{sel}}}
\newcommand{\SelTarget}[1]{\mathcal R_{#1}^{\mathrm{sel}}}
\newcommand{\SelTargetMetric}[1]{r_{#1}^{\mathrm{sel}}}
\newcommand{\SelGap}[1]{\zeta_{#1}^{\mathrm{sel}}}
\newcommand{\SelSection}[1]{\mathfrak M_{#1}^{\mathrm{sel}}}
\newcommand{\SelFunctional}[1]{\mathscr S_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedPreProto}[1]{\overline{\mathfrak U}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedSubprimitive}[1]{\overline{\mathfrak T}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedPrimitive}[1]{\overline{\mathfrak G}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedRoot}[1]{\overline{\mathfrak P}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedPreAmbient}[1]{\overline{\mathfrak Z}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedAmbient}[1]{\overline{\mathfrak S}_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiftedShell}[1]{\overline{\mathfrak D}_{#1}^{\mathrm{sel}}}
\newcommand{\SelConstraintMap}[1]{\overline{\mathfrak C}_{#1}^{\mathrm{sel}}}
\newcommand{\SelCotangentRealization}[1]{\Upsilon_{#1}^{\mathrm{sel}}}
\newcommand{\SelLiouville}[1]{\boldsymbol{\Lambda}_{#1}^{\mathrm{sel}}}
\newcommand{\SelTwo}[1]{\boldsymbol{\Xi}_{#1}^{\mathrm{sel}}}
\newcommand{\SelConnectionForm}[1]{\mathcal G_{#1}^{\mathrm{conn}}}
\newcommand{\SelTransport}[1]{\mathbb Y_{#1}}
\newcommand{\SelShadowForm}[1]{\boldsymbol{\varsigma}_{#1}^{\mathrm{sel}}}
\newcommand{\SelRateForm}[1]{\boldsymbol{\lambda}_{#1}^{\mathrm{sel}}}
\newcommand{\SelDensitySource}[1]{\boldsymbol{q}_{#1}^{\mathrm{sel}}}
\newcommand{\SelFluxSource}[1]{\boldsymbol{j}_{#1}^{\mathrm{sel}}}
\newcommand{\SelBoundarySource}[1]{\boldsymbol{\rho}_{#1}^{\partial,\mathrm{sel}}}
\newcommand{\SelAction}[1]{\mathscr A_{#1}^{\mathrm{sel}}}

\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}
\newtheorem{corollary}{Corollary}
\newtheorem{theorem}{Theorem}

\title{\textbf{The \TheoryName{}}\\[0.4em]
\large Substrate-Response / Self-Response Charge-Polarization Candidate\\
\large Exact-Preservation Pre-Proto-Subprimitive Stationary-Selection / Cotangent-Connection / Source-Action\\
\large Origin Note}
\author{}
\date{}

\begin{document}

\maketitle

\begin{abstract}
The exact-preservation proto-subprimitive-substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport origin note already pushed the main one-metric charge-polarization line one layer below the recorded subprimitive package. It showed that the recorded proto-subprimitive layer is itself the exact shell-restricted image of a deeper pre-proto-subprimitive layer carrying pre-proto-subprimitive substrate bundles, proto-subprimitive-generating shell sections, lifted subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps into the same target metrics, pre-proto-subprimitive cotangent-model Liouville one-forms and closed two-form geometry, pre-proto-subprimitive connection one-forms and induced parallel transports, and pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data together with fixed-endpoint pre-proto-subprimitive rate-action geometry. What remained open was the next honest burden below that note: why the pre-proto-subprimitive layer itself should exist at all rather than becoming the new deepest disciplined postulate.

The present note addresses exactly that narrower burden. It keeps fixed the same exact-preservation target: the same \(\StemFunctional\), the same exact-preservation normal form \(\DeepFunctional\), the same carrier-origin / primitive-reservoir / microscopic-equilibrium functionals
\[
\CarrierFunctional[\CarrierSeed,\RelabelSeed,\FreeSeed],\qquad
\PrimFunctional[\PrimStrain,\PrimNeutral,\PrimFree],\qquad
\MicFunctional[H,\GaugeAux],
\]
the same \(\Sigma^{\mathrm{cmp}}\) solve, the same \(\Pi_{\mathrm{free}}H=0\) outcome, the same observer-equation closure, the same one operative metric, and the same recorded Phase D / E and Phase F gains. Its positive move is not another packaging pass and not stronger promotion language. Instead it introduces a deeper exact-preserving stationary-selection layer:
\begin{enumerate}
    \item finite-dimensional compact-visible selection bundles with preserved readouts, unit sections, time reversals, and positive metrics;
    \item exact-preserving stationary-selection sections whose zero loci are exactly the recorded pre-proto-subprimitive substrate bundles and whose norm-square functionals make those bundles the selected zero-energy stationary states with positive normal gap;
    \item lifted proto-subprimitive-generating shell maps, lifted subprimitive-generating shell maps, lifted primitive-generating shell maps, lifted root-generating shell maps, lifted pre-ambient-generating shell maps, lifted ambient-generating shell maps, lifted shell maps, and lifted exact-constraint maps whose pre-proto-subprimitive-shell restrictions are exactly the recorded pre-proto-subprimitive maps;
    \item selection cotangent-model Liouville one-forms and closed two-form geometry whose pre-proto-subprimitive-shell restrictions are exactly the recorded pre-proto-subprimitive Liouville and closed two-form geometry;
    \item exact-preserving unitary selection connection one-forms whose parallel transports along selection curves restrict exactly to the recorded pre-proto-subprimitive connection one-forms and their induced parallel transports;
    \item selection-basic shadow / rate / density / flux / boundary data and fixed-endpoint selection actions whose restrictions are exactly the recorded pre-proto-subprimitive-basic data and the same fixed-endpoint pre-proto-subprimitive rate-action geometry.
\end{enumerate}

At current exact-preservation scope, the pre-proto-subprimitive substrate bundles, their proto-subprimitive-generating shell sections, their lifted subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps, their pre-proto-subprimitive Liouville / closed-two-form geometry, their pre-proto-subprimitive connection one-forms and induced parallel transports, and their pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data together with their fixed-endpoint pre-proto-subprimitive rate-action geometry are therefore no longer primitive either. Because the present note reproduces the full input layer of the immediately preceding proto-subprimitive note without change, that note applies verbatim. Consequently the same proto-subprimitive layer, the same subprimitive layer, the same primitive layer, the same root layer, the same pre-ambient layer, the same ambient-shell / cotangent-Liouville / connection-holonomy layer, the same process-state and reversible-chain package, the same \(\StemFunctional\), the same exact-preservation normal form, the same carrier-origin / primitive-reservoir / microscopic-equilibrium functionals, the same \(\Sigma^{\mathrm{cmp}}\) solve, the same \(\Pi_{\mathrm{free}}H=0\) outcome, the same observer-equation closure, the same one operative metric, and the same recorded Phase D / E and Phase F gains are preserved without change.

The scope remains disciplined. This note does not claim a final microscopic completion, a uniqueness theorem for every deeper realization, or a completed first-principles derivation of GR. Its narrower exact positive claim is only that the recorded pre-proto-subprimitive layer is itself no longer primitive at present scope, but is selected by a still deeper exact-preserving stationary-selection / cotangent-connection / source-action layer while preserving the same downstream package.
\end{abstract}

\section{Introduction}

The active one-metric charge-polarization line has now crossed the candidate, benchmark-gate, controlled-limit, residual-sector, redesign, substrate-anchoring, substrate-action, kernel-origin, deeper-selection, primitive-reservoir, carrier-origin, precursor-selection, exact-preservation normal-form, microphysical-Hessian, charge-generator, derivation-algebra, reconfiguration-group, reversible-groupoid, reversible-history, reversible process-window, reversible process-thread, process-state, proto-process, constraint-shell / Liouville-holonomy, ambient-shell / cotangent-connection, pre-ambient-shell / connection-cotangent / parallel-transport, root-shell / lifted connection-cotangent / parallel-transport, primitive-substrate / root-generating shell / lifted connection-cotangent / parallel-transport, subprimitive-substrate / primitive-generating shell / lifted connection-cotangent / parallel-transport, and proto-subprimitive-substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport origin steps on the same GR-directed route. The burden therefore sharpens again. It is still not another packaging pass. It is still not stronger promotion language. It is still not, by default, a move back onto the anchored positive-pair side line. It is to go one layer deeper again below the newly recorded pre-proto-subprimitive construction itself and explain why that layer is forced.

That burden has ten parts. First, one must explain why the exact-preserving pre-proto-subprimitive substrate bundles
\[
\PreProtoSubprimSpace{\Car},\qquad
\PreProtoSubprimSpace{\Cur},\qquad
\PreProtoSubprimSpace{\Hom}
\]
should exist rather than being simply declared. Second, one must explain why the visible pre-proto-subprimitive readout maps, pre-proto-subprimitive unit sections, pre-proto-subprimitive time reversals, preserved positive pre-proto-subprimitive metrics, and compact visible pre-proto-subprimitive fibers should be forced rather than inserted independently. Third, one must explain why the proto-subprimitive-generating shell sections
\[
\PreProtoSubprimSection{\Car},\qquad
\PreProtoSubprimSection{\Cur},\qquad
\PreProtoSubprimSection{\Hom}
\]
should be forced rather than postulated. Fourth, one must explain why the lifted subprimitive-generating shell maps
\[
\PreProtoSubprimLiftedSubprimitive{\Car},\qquad
\PreProtoSubprimLiftedSubprimitive{\Cur},\qquad
\PreProtoSubprimLiftedSubprimitive{\Hom}
\]
and the lifted primitive-generating shell maps
\[
\PreProtoSubprimLiftedPrimitive{\Car},\qquad
\PreProtoSubprimLiftedPrimitive{\Cur},\qquad
\PreProtoSubprimLiftedPrimitive{\Hom}
\]
should themselves be induced rather than separately chosen. Fifth, one must explain why the lifted root-generating shell maps, lifted pre-ambient-generating shell maps, lifted ambient-generating shell maps, lifted shell maps, and lifted exact-constraint maps into the same target metrics should themselves be induced rather than separately chosen. Sixth, one must explain why the pre-proto-subprimitive cotangent-model Liouville one-forms, closed two-form geometry, pre-proto-subprimitive connection one-forms, and induced parallel transports should themselves be induced rather than separately prescribed. Seventh, one must explain why the pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data and the fixed-endpoint pre-proto-subprimitive rate-action geometry should be forced rather than added as independent decorations. Eighth, one must re-show that the same proto-subprimitive layer, the same subprimitive layer, the same primitive layer, the same root layer, the same pre-ambient layer, the same ambient-shell / cotangent-Liouville / connection-holonomy layer, the same process-state and reversible-chain package, the same \(\StemFunctional\), the same exact-preservation normal form, the same carrier-origin / primitive-reservoir / microscopic-equilibrium functionals, the same \(\Sigma^{\mathrm{cmp}}\) solve, the same \(\Pi_{\mathrm{free}}H=0\) outcome, the same observer-equation closure, the same one operative metric, and the same recorded Phase D / E and Phase F gains remain intact. Ninth, one must explain why the sharper move is a stationary-selection justification rather than another blind relabeling of a deeper shell. Tenth, one must keep the claim boundary honest while doing so.

The positive claim of the present note is that there is a disciplined way to do exactly that. One introduces a deeper exact-preserving stationary-selection bundle whose visible fibers are compact, whose exact stationary zero shells are precisely the recorded pre-proto-subprimitive substrate bundles, whose lifted proto-subprimitive-generating / subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps restrict exactly to the recorded pre-proto-subprimitive maps, whose cotangent-model one-forms descend by shell restriction to the recorded pre-proto-subprimitive Liouville geometry, whose skew-Hermitian selection connection one-forms restrict to the recorded pre-proto-subprimitive connection one-forms and generate the recorded pre-proto-subprimitive transports by parallel transport, and whose selection-basic one-forms / source fields / boundary potentials together with the same fixed-endpoint variational structure restrict to the recorded pre-proto-subprimitive-basic data. The immediately preceding proto-subprimitive note can then be invoked without alteration, so the whole downstream chain is preserved verbatim.

\section{Fixed Inputs from the Recorded Completion Line}

\begin{definition}[Fixed branch and downstream completion target]
\label{def:fixed_branch}
The present note keeps fixed the same charge-polarization branch
\begin{equation}
\CandidateMetric
=
\ObserverMetric+\BaseShift+\ResponseShift,
\qquad
\CompletedMetric
=
\ObserverMetric+\BaseShift+\ResponseShift+\CompShift,
\label{eq:fixed_metrics}
\end{equation}
with lifted variables satisfying
\begin{equation}
\ObserverMap^*\BaseLift=\BaseShift,
\qquad
\ObserverMap^*\ResponseLift=\ResponseShift,
\qquad
\ObserverMap^*\CompLift=\CompShift.
\label{eq:fixed_lifts}
\end{equation}
The same completion class and the same free-sector condition are fixed:
\begin{equation}
\CompLift\in\CompClass,
\qquad
\FreeProj\CompLift=0.
\label{eq:fixed_free}
\end{equation}
The same substrate and observer completion equations are fixed as well:
\begin{equation}
\SubReducedEin(\CompLift)=-\ResidualLift,
\qquad
\ReducedEin(\CompShift)=-\ResidualCore.
\label{eq:fixed_solves}
\end{equation}
Every statement below must preserve these equations together with the same one operative metric \(\CompletedMetric\).
\end{definition}

\begin{definition}[Recorded pre-proto-subprimitive geometry]
\label{def:recorded_preproto_geometry}
The immediately preceding proto-subprimitive-substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport origin note fixes the following exact-preserving input layer for every sector label \(\sigma\in\SectorSet=\{\Car,\Cur,\Hom\}\).
\begin{enumerate}
    \item Finite-dimensional exact-preserving pre-proto-subprimitive substrate bundles
    \[
    \PreProtoSubprimSpace{\sigma}
    \]
    with visible pre-proto-subprimitive readout maps
    \[
    \PreProtoSubprimRead{\sigma}:\PreProtoSubprimSpace{\sigma}\to\ObjClass{\sigma},
    \]
    exact-preserving pre-proto-subprimitive unit sections
    \[
    \PreProtoSubprimUnit{\sigma}:\ObjClass{\sigma}\to\PreProtoSubprimSpace{\sigma},
    \]
    involutive exact-preserving pre-proto-subprimitive time reversals
    \[
    \PreProtoSubprimTimeRev{\sigma}:\PreProtoSubprimSpace{\sigma}\to\PreProtoSubprimSpace{\sigma},
    \]
    and preserved positive pre-proto-subprimitive metrics
    \[
    \PreProtoSubprimMetric{\sigma}.
    \]
    Each visible pre-proto-subprimitive fiber \((\PreProtoSubprimRead{\sigma})^{-1}(x)\) is compact.
    \item Finite-dimensional real pre-proto-subprimitive target spaces
    \[
    \PreProtoSubprimTarget{\sigma}
    \]
    with positive metrics
    \[
    \PreProtoSubprimTargetMetric{\sigma},
    \]
    and exact-preserving proto-subprimitive-generating shell sections
    \[
    \PreProtoSubprimSection{\sigma}:\PreProtoSubprimSpace{\sigma}\to\PreProtoSubprimTarget{\sigma}
    \]
    whose zero loci are exactly the current proto-subprimitive substrate bundles \(\ProtoSubprimSpace{\sigma}\). These sections carry the same recorded positive pre-proto-subprimitive gap bounds \(\PreProtoSubprimGap{\sigma}>0\) on the current proto-subprimitive shells.
    \item Exact-preserving lifted subprimitive-generating shell maps
    \[
    \PreProtoSubprimLiftedSubprimitive{\sigma}:\PreProtoSubprimSpace{\sigma}\to\ProtoSubprimTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\ProtoSubprimTargetMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded subprimitive-generating shell sections \(\ProtoSubprimSection{\sigma}\) and carry the same recorded positive proto-subprimitive gap bounds \(\ProtoSubprimGap{\sigma}>0\) on the current subprimitive shells.
    \item Exact-preserving lifted primitive-generating shell maps
    \[
    \PreProtoSubprimLiftedPrimitive{\sigma}:\PreProtoSubprimSpace{\sigma}\to\SubprimTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\SubprimTargetMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded lifted primitive-generating shell maps \(\ProtoSubprimLiftedPrimitive{\sigma}\) and carry the same recorded positive subprimitive gap bounds \(\SubprimGap{\sigma}>0\) on the current primitive shells.
    \item Exact-preserving lifted root-generating shell maps
    \[
    \PreProtoSubprimLiftedRoot{\sigma}:\PreProtoSubprimSpace{\sigma}\to\PrimTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\PrimTargetMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded lifted root-generating shell maps \(\ProtoSubprimLiftedRoot{\sigma}\) and carry the same recorded positive primitive gap bounds \(\PrimGap{\sigma}>0\) on the current root shells.
    \item Exact-preserving lifted pre-ambient-generating shell maps
    \[
    \PreProtoSubprimLiftedPreAmbient{\sigma}:\PreProtoSubprimSpace{\sigma}\to\RootTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\RootTargetMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded lifted pre-ambient-generating shell maps \(\ProtoSubprimLiftedPreAmbient{\sigma}\) and carry the same recorded positive root gap bounds \(\RootGap{\sigma}>0\) on the current pre-ambient shells.
    \item Exact-preserving lifted ambient-generating shell maps
    \[
    \PreProtoSubprimLiftedAmbient{\sigma}:\PreProtoSubprimSpace{\sigma}\to\OriginTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\OriginTargetMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded lifted ambient-generating shell maps \(\ProtoSubprimLiftedAmbient{\sigma}\) and carry the same recorded positive ambient-generating gap bounds \(\OriginGap{\sigma}>0\) on the current ambient shells.
    \item Exact-preserving lifted shell maps
    \[
    \PreProtoSubprimLiftedShell{\sigma}:\PreProtoSubprimSpace{\sigma}\to\ShellTarget{\sigma}
    \]
    into the same positive shell target spaces with the same positive shell target metrics \(\ShellMetric{\sigma}\). Their restrictions to the current proto-subprimitive exact-constraint shells are exactly the recorded lifted shell maps \(\ProtoSubprimLiftedShell{\sigma}\) and carry the same recorded positive shell-gap bounds \(\ShellGap{\sigma}>0\) on the current seed shells.
    \item Exact-preserving lifted exact-constraint maps
    \[
    \PreProtoSubprimConstraintMap{\sigma}:\PreProtoSubprimSpace{\sigma}\to\ConstraintTarget{\sigma}
    \]
    into the same positive target spaces with the same positive target metrics \(\ConstraintMetric{\sigma}\). Their restrictions to the current proto-subprimitive shells are exactly the recorded lifted exact-constraint maps \(\ProtoSubprimConstraintMap{\sigma}\) and carry the same recorded positive exact-constraint gap bounds \(\ConstraintGap{\sigma}>0\).
    \item Exact-preserving cotangent realizations
    \[
    \PreProtoSubprimCotangentRealization{\sigma}:\PreProtoSubprimSpace{\sigma}\to T^*\ConfigSpace{\sigma}
    \]
    producing the pre-proto-subprimitive Liouville one-forms
    \[
    \PreProtoSubprimLiouville{\sigma}
    \]
    and the closed pre-proto-subprimitive two-forms
    \[
    \PreProtoSubprimTwo{\sigma}.
    \]
    Their proto-subprimitive-shell restrictions are exactly the recorded proto-subprimitive Liouville one-forms \(\ProtoSubprimLiouville{\sigma}\) and proto-subprimitive closed two-form geometry \(\ProtoSubprimTwo{\sigma}\).
    \item Exact-preserving skew-Hermitian pre-proto-subprimitive connection one-forms
    \[
    \PreProtoSubprimConnectionForm{\sigma}\in \Omega^1(\PreProtoSubprimSpace{\sigma};\LieU(\RootClass{\sigma}))
    \]
    and the induced exact-preserving unitary parallel transports
    \[
    \PreProtoSubprimTransport{\sigma}
    \]
    attached to piecewise \(C^1\) pre-proto-subprimitive curves, identity on constant curves, multiplicative under concatenation, inverse under the corresponding pre-proto-subprimitive time reversals, and exact-preserving on the fixed branch. Their proto-subprimitive-shell restrictions are exactly the recorded proto-subprimitive connection one-forms \(\ProtoSubprimConnectionForm{\sigma}\) and induced proto-subprimitive parallel transports \(\ProtoSubprimTransport{\sigma}\).
\end{enumerate}
These data are exact fixed inputs for the present note. The only admissible positive result is to derive or justify why this full layer is itself forced while keeping it unchanged.
\end{definition}

\begin{definition}[Recorded pre-proto-subprimitive basic source and fixed-endpoint action data]
\label{def:recorded_preproto_basic}
Assume the recorded pre-proto-subprimitive geometry of Definition \ref{def:recorded_preproto_geometry} is fixed. For each \(\sigma\in\SectorSet\), the same recorded note fixes:
\begin{enumerate}
    \item exact-preserving pre-proto-subprimitive shadow one-forms
    \[
    \PreProtoSubprimShadowForm{\sigma}\in\Omega^1(\PreProtoSubprimSpace{\sigma};\StatTagClass{\sigma}),
    \]
    invariant under the corresponding pre-proto-subprimitive time reversals, annihilating vectors normal to the proto-subprimitive-generating shell locus \(\ProtoSubprimSpace{\sigma}\), annihilating vertical vectors tangent to the visible pre-proto-subprimitive fibers, and preserved along the current proto-subprimitive characteristic curves singled out on the primitive exact-constraint shells by the restricted proto-subprimitive Liouville / exact-constraint geometry;
    \item exact-preserving pre-proto-subprimitive rate one-forms
    \[
    \PreProtoSubprimRateForm{\sigma}\in\Omega^1(\PreProtoSubprimSpace{\sigma}),
    \]
    with the same invariance and annihilation properties;
    \item exact-preserving pre-proto-subprimitive density and pre-proto-subprimitive flux source fields
    \[
    \PreProtoSubprimDensitySource{\sigma}:\PreProtoSubprimSpace{\sigma}\to\DensityClass{\sigma},
    \qquad
    \PreProtoSubprimFluxSource{\sigma}:\PreProtoSubprimSpace{\sigma}\to\FluxClass{\sigma},
    \]
    invariant under the corresponding pre-proto-subprimitive time reversals, preserved along the current proto-subprimitive characteristic curves, and constant on proto-subprimitive-shell-normal directions and on vertical pre-proto-subprimitive-fiber directions;
    \item exact-preserving pre-proto-subprimitive boundary potentials
    \[
    \PreProtoSubprimBoundarySource{\sigma}:\PreProtoSubprimSpace{\sigma}\to\PhaseReadClass{\sigma},
    \]
    vanishing on the corresponding pre-proto-subprimitive unit sections, invariant under the corresponding pre-proto-subprimitive time reversals, annihilating vectors normal to the proto-subprimitive-generating shells, and constant on vertical pre-proto-subprimitive-fiber directions;
    \item fixed-endpoint pre-proto-subprimitive actions
    \[
    \PreProtoSubprimAction{\sigma}
    \]
    on curves constrained to the current root exact-constraint shells inside the current primitive-generating shells and current subprimitive-generating shells, whose stationary curves are exactly the curves singled out there by the restricted pre-proto-subprimitive Liouville / exact-constraint geometry, whose first variations vanish automatically along the characteristic, symmetry, vertical proto-subprimitive-fiber, and proto-subprimitive-shell-normal directions, and whose second variations are positive on smooth endpoint-fixed complements to those directions and to the normal exact-constraint directions singled out by \(\ConstraintGap{\sigma}\).
\end{enumerate}
\end{definition}

\begin{definition}[Recorded exact-preservation target]
\label{def:recorded_target}
The present note must preserve, without any modification,
\begin{equation}
\StemFunctional,
\qquad
\DeepFunctional,
\qquad
\CarrierFunctional[\CarrierSeed,\RelabelSeed,\FreeSeed],
\qquad
\PrimFunctional[\PrimStrain,\PrimNeutral,\PrimFree],
\qquad
\MicFunctional[H,\GaugeAux],
\label{eq:target_functionals}
\end{equation}
together with the fixed free-sector verdict
\begin{equation}
\FreeProj\CompLift=0,
\label{eq:target_free}
\end{equation}
and hence the same previously recorded \(\Pi_{\mathrm{free}}H=0\) outcome, the same completion solve
\begin{equation}
\CompShift=\Sigma^{\mathrm{cmp}},
\label{eq:target_sigma}
\end{equation}
the same one operative metric \(\CompletedMetric\), and the same recorded Phase D / E infrared-spectrum and universal-coupling verdicts together with the same Phase F controlled GR limit.
\end{definition}

\section{Stationary-Selection Origin of the Recorded Pre-Proto-Subprimitive Geometry}

\begin{definition}[Deeper stationary-selection bundles and lifted geometry]
\label{def:selection_geometry}
For each sector label \(\sigma\in\SectorSet\), let
\[
\SelSpace{\sigma}
\]
be a finite-dimensional \(C^2\) manifold of yet more primitive exact-preserving substrate states. Assume:
\begin{enumerate}
    \item there are smooth visible selection readout maps
    \[
    \SelRead{\sigma}:\SelSpace{\sigma}\to\ObjClass{\sigma},
    \]
    together with exact-preserving selection unit sections
    \[
    \SelUnit{\sigma}:\ObjClass{\sigma}\to\SelSpace{\sigma}
    \]
    satisfying
    \[
    \SelRead{\sigma}\circ\SelUnit{\sigma}=\mathrm{id}_{\ObjClass{\sigma}}.
    \]
    \item there are smooth involutive exact-preserving selection time reversals
    \[
    \SelTimeRev{\sigma}:\SelSpace{\sigma}\to\SelSpace{\sigma}
    \]
    such that
    \[
    \SelRead{\sigma}\circ\SelTimeRev{\sigma}=\SelRead{\sigma},
    \qquad
    \SelTimeRev{\sigma}\circ\SelUnit{\sigma}=\SelUnit{\sigma}.
    \]
    \item each selection space carries a smooth exact-preserving positive-definite metric
    \[
    \SelMetric{\sigma}
    \]
    preserved by the corresponding selection time reversal, and each visible selection fiber \((\SelRead{\sigma})^{-1}(x)\) is compact.
    \item there are finite-dimensional real stationary-selection target spaces
    \[
    \SelTarget{\sigma}
    \]
    with positive-definite metrics
    \[
    \SelTargetMetric{\sigma},
    \]
    and exact-preserving smooth stationary-selection sections
    \[
    \SelSection{\sigma}:\SelSpace{\sigma}\to\SelTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the stationary-selection functionals
    \begin{equation}
    \SelFunctional{\sigma}(y)\equiv \frac12\,
    \SelTargetMetric{\sigma}\bigl(\SelSection{\sigma}(y),\SelSection{\sigma}(y)\bigr).
    \label{eq:selection_functional}
    \end{equation}
    Define the recorded pre-proto-subprimitive substrate bundles as the exact zero shells
    \begin{equation}
    \PreProtoSubprimSpace{\sigma}\equiv \left(\SelSection{\sigma}\right)^{-1}(0),
    \label{eq:preproto_shell_from_selection}
    \end{equation}
    and let
    \begin{equation}
    \PreProtoSubprimInc{\sigma}:\PreProtoSubprimSpace{\sigma}\hookrightarrow \SelSpace{\sigma}
    \label{eq:preproto_inclusion}
    \end{equation}
    be the shell inclusion. Assume this shell is a closed embedded \(C^2\) submanifold, invariant under the corresponding selection time reversal, containing the image of the corresponding selection unit section, and equal to the exact set of global minima of \(\SelFunctional{\sigma}\). Assume further that, for every \(x\in\PreProtoSubprimSpace{\sigma}\),
    \begin{equation}
    \SelTargetMetric{\sigma}\!\left(
    D\SelSection{\sigma}(x)\xi,
    D\SelSection{\sigma}(x)\xi
    \right)
    \ge
    \SelGap{\sigma}\,
    \SelMetric{\sigma}(\xi,\xi),
    \qquad
    \SelGap{\sigma}>0,
    \label{eq:selection_gap}
    \end{equation}
    for all \(\xi\) orthogonal, with respect to \(\SelMetric{\sigma}\), to \(T_x\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted proto-subprimitive-generating shell maps
    \[
    \SelLiftedPreProto{\sigma}:\SelSpace{\sigma}\to\PreProtoSubprimTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded proto-subprimitive-generating shell sections by restriction:
    \begin{equation}
    \PreProtoSubprimSection{\sigma}\equiv \SelLiftedPreProto{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_section_from_selection}
    \end{equation}
    Assume these restricted sections satisfy the same recorded positive pre-proto-subprimitive gap bounds \(\PreProtoSubprimGap{\sigma}>0\) on the current proto-subprimitive shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted subprimitive-generating shell maps
    \[
    \SelLiftedSubprimitive{\sigma}:\SelSpace{\sigma}\to\ProtoSubprimTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted subprimitive-generating shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedSubprimitive{\sigma}\equiv \SelLiftedSubprimitive{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_subprimitive_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive proto-subprimitive gap bounds \(\ProtoSubprimGap{\sigma}>0\) on the current subprimitive shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted primitive-generating shell maps
    \[
    \SelLiftedPrimitive{\sigma}:\SelSpace{\sigma}\to\SubprimTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted primitive-generating shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedPrimitive{\sigma}\equiv \SelLiftedPrimitive{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_primitive_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive subprimitive gap bounds \(\SubprimGap{\sigma}>0\) on the current primitive shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted root-generating shell maps
    \[
    \SelLiftedRoot{\sigma}:\SelSpace{\sigma}\to\PrimTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted root-generating shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedRoot{\sigma}\equiv \SelLiftedRoot{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_root_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive primitive gap bounds \(\PrimGap{\sigma}>0\) on the current root shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted pre-ambient-generating shell maps
    \[
    \SelLiftedPreAmbient{\sigma}:\SelSpace{\sigma}\to\RootTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted pre-ambient-generating shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedPreAmbient{\sigma}\equiv \SelLiftedPreAmbient{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_preambient_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive root gap bounds \(\RootGap{\sigma}>0\) on the current pre-ambient shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted ambient-generating shell maps
    \[
    \SelLiftedAmbient{\sigma}:\SelSpace{\sigma}\to\OriginTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted ambient-generating shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedAmbient{\sigma}\equiv \SelLiftedAmbient{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_ambient_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive ambient-generating gap bounds \(\OriginGap{\sigma}>0\) on the current ambient shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted shell maps
    \[
    \SelLiftedShell{\sigma}:\SelSpace{\sigma}\to\ShellTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted shell maps by restriction:
    \begin{equation}
    \PreProtoSubprimLiftedShell{\sigma}\equiv \SelLiftedShell{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_shell_map_from_selection}
    \end{equation}
    Assume these restricted maps satisfy the same recorded positive shell-gap bounds \(\ShellGap{\sigma}>0\) on the current seed shells cut out inside \(\PreProtoSubprimSpace{\sigma}\).
    \item there are exact-preserving lifted exact-constraint maps
    \[
    \SelConstraintMap{\sigma}:\SelSpace{\sigma}\to\ConstraintTarget{\sigma}
    \]
    preserved by the corresponding selection time reversals and vanishing on the corresponding selection unit sections. Define the recorded lifted exact-constraint maps by restriction:
    \begin{equation}
    \PreProtoSubprimConstraintMap{\sigma}\equiv \SelConstraintMap{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_constraint_from_selection}
    \end{equation}
    \item there are exact-preserving cotangent realizations
    \[
    \SelCotangentRealization{\sigma}:\SelSpace{\sigma}\to T^*\ConfigSpace{\sigma}
    \]
    into the same recorded configuration spaces. Let \(\theta_{\mathrm{can}}\) denote the canonical Liouville one-form on the relevant cotangent bundle. Define selection Liouville one-forms and closed two-forms by
    \begin{equation}
    \SelLiouville{\sigma}\equiv (\SelCotangentRealization{\sigma})^*\theta_{\mathrm{can}},
    \qquad
    \SelTwo{\sigma}\equiv d\SelLiouville{\sigma}.
    \label{eq:selection_liouville}
    \end{equation}
    Assume
    \[
    (\SelTimeRev{\sigma})^*\SelLiouville{\sigma}
    =
    -\SelLiouville{\sigma}.
    \]
    Define the recorded pre-proto-subprimitive Liouville one-forms and closed two-forms by shell restriction:
    \begin{equation}
    \PreProtoSubprimLiouville{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelLiouville{\sigma},
    \qquad
    \PreProtoSubprimTwo{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelTwo{\sigma}.
    \label{eq:preproto_liouville_from_selection}
    \end{equation}
    \item there are smooth skew-Hermitian selection connection one-forms
    \[
    \SelConnectionForm{\sigma}\in \Omega^1(\SelSpace{\sigma};\LieU(\RootClass{\sigma}))
    \]
    that are exact-preserving on the fixed branch and satisfy the time-reversal equivariance rule
    \[
    (\SelTimeRev{\sigma})^*\SelConnectionForm{\sigma}
    =
    -\SelConnectionForm{\sigma}.
    \]
    Let
    \[
    \SelTransport{\sigma}(\Gamma)\in \mathcal U(\RootClass{\sigma})
    \]
    denote the parallel transport generated by that selection connection one-form along any piecewise \(C^1\) selection curve \(\Gamma\). Define the recorded pre-proto-subprimitive connection one-forms and induced parallel transports by
    \begin{equation}
    \PreProtoSubprimConnectionForm{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelConnectionForm{\sigma},
    \label{eq:preproto_connection_from_selection}
    \end{equation}
    \begin{equation}
    \PreProtoSubprimTransport{\sigma}(\gamma)\equiv \SelTransport{\sigma}(\PreProtoSubprimInc{\sigma}\circ\gamma)
    \label{eq:preproto_transport_from_selection}
    \end{equation}
    on piecewise \(C^1\) pre-proto-subprimitive curves \(\gamma\).
\end{enumerate}

Define the recorded pre-proto-subprimitive readout maps, pre-proto-subprimitive unit sections, pre-proto-subprimitive time reversals, and pre-proto-subprimitive metrics by restriction:
\begin{equation}
\PreProtoSubprimRead{\sigma}\equiv \SelRead{\sigma}\circ\PreProtoSubprimInc{\sigma},
\qquad
\PreProtoSubprimUnit{\sigma}\equiv \SelUnit{\sigma},
\qquad
\PreProtoSubprimTimeRev{\sigma}\equiv \SelTimeRev{\sigma}|_{\PreProtoSubprimSpace{\sigma}},
\label{eq:preproto_basic_structure_from_selection}
\end{equation}
\begin{equation}
\PreProtoSubprimMetric{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelMetric{\sigma}.
\label{eq:preproto_metric_from_selection}
\end{equation}
For each visible point, define the visible pre-proto-subprimitive fibers by
\begin{equation}
(\PreProtoSubprimRead{\sigma})^{-1}(x)
=
\left(\SelSection{\sigma}\right)^{-1}(0)\cap(\SelRead{\sigma})^{-1}(x).
\label{eq:preproto_fiber_from_selection}
\end{equation}
\end{definition}

\begin{proposition}[Pre-proto-subprimitive substrate bundles, shell sections, lifted maps, cotangent geometry, and connection-transports are forced]
\label{prop:preproto_geometry_forced}
The stationary-selection data of Definition \ref{def:selection_geometry} recover exactly the geometric part of the recorded pre-proto-subprimitive layer.
\begin{enumerate}
    \item The exact zero shells \eqref{eq:preproto_shell_from_selection} are exactly the recorded pre-proto-subprimitive substrate bundles \(\PreProtoSubprimSpace{\sigma}\), and they are exactly the global-minimum stationary loci of the selection functionals \(\SelFunctional{\sigma}\).
    \item The restricted maps \eqref{eq:preproto_basic_structure_from_selection}--\eqref{eq:preproto_metric_from_selection} are exactly the recorded pre-proto-subprimitive readout maps, pre-proto-subprimitive unit sections, pre-proto-subprimitive time reversals, and preserved positive pre-proto-subprimitive metrics, and the visible pre-proto-subprimitive fibers \eqref{eq:preproto_fiber_from_selection} are compact.
    \item The restricted maps \eqref{eq:preproto_section_from_selection} are exactly the recorded proto-subprimitive-generating shell sections, and their pre-proto-subprimitive gap bounds are the same recorded bounds \(\PreProtoSubprimGap{\sigma}\) on the current proto-subprimitive shells.
    \item The restricted maps \eqref{eq:preproto_subprimitive_from_selection} are exactly the recorded lifted subprimitive-generating shell maps, and their proto-subprimitive gap bounds are the same recorded bounds \(\ProtoSubprimGap{\sigma}\) on the current subprimitive shells.
    \item The restricted maps \eqref{eq:preproto_primitive_from_selection} are exactly the recorded lifted primitive-generating shell maps, and their subprimitive gap bounds are the same recorded bounds \(\SubprimGap{\sigma}\) on the current primitive shells.
    \item The restricted maps \eqref{eq:preproto_root_from_selection} and \eqref{eq:preproto_preambient_from_selection} are exactly the recorded lifted root-generating shell maps and lifted pre-ambient-generating shell maps, with the same recorded primitive and root gap bounds.
    \item The restricted maps \eqref{eq:preproto_ambient_from_selection}, \eqref{eq:preproto_shell_map_from_selection}, and \eqref{eq:preproto_constraint_from_selection} are exactly the recorded lifted ambient-generating shell maps, lifted shell maps, and lifted exact-constraint maps with the same target metrics and the same recorded ambient, shell, and exact-constraint gap data.
    \item The restricted forms \eqref{eq:preproto_liouville_from_selection} are exactly the recorded pre-proto-subprimitive Liouville one-forms and closed two-form geometry.
    \item The restricted connection one-forms and parallel-transport formulas \eqref{eq:preproto_connection_from_selection}--\eqref{eq:preproto_transport_from_selection} are exactly the recorded pre-proto-subprimitive connection one-forms and their induced pre-proto-subprimitive parallel transports.
    \item Consequently the pre-proto-subprimitive substrate bundles, their proto-subprimitive-generating shell sections, their lifted subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps, their pre-proto-subprimitive Liouville / closed-two-form geometry, and their pre-proto-subprimitive connection one-forms with induced parallel transports are no longer primitive at present scope; they are forced by a deeper stationary-selection / lifted-geometry / cotangent-connection layer.
\end{enumerate}
\end{proposition}

\begin{proof}
Item (1) is the stationary-selection construction itself. By assumption, the zero shell of \(\SelSection{\sigma}\) is a closed embedded \(C^2\) submanifold, invariant under the corresponding selection time reversal and containing the image of the corresponding unit section. The norm-square functional \eqref{eq:selection_functional} has this shell as its exact zero-energy locus, and the positive normal bound \eqref{eq:selection_gap} is precisely the current-scope statement that the selected shell is not arbitrary but is the stable stationary zero set of the deeper selection principle. That is exactly the justification burden for the existence of the recorded pre-proto-subprimitive substrate bundle.

Item (2) follows by direct restriction along the shell inclusion \(\PreProtoSubprimInc{\sigma}\). The restriction of a positive-definite metric to an embedded submanifold is positive definite, and compactness of the visible pre-proto-subprimitive fibers follows because they are closed subsets of compact visible selection fibers.

Items (3)--(7) are immediate from the defining restriction formulas. The target spaces and target metrics are already fixed to be the same recorded ones, and the recorded gap bounds are assumed to survive unchanged on the appropriate descendant shells. Thus no visible slot of the recorded pre-proto-subprimitive shell and lifted-map package is altered.

Item (8) follows from functoriality of pullback and exterior differentiation:
\[
\PreProtoSubprimTwo{\sigma}
=
(\PreProtoSubprimInc{\sigma})^*d\SelLiouville{\sigma}
=
d(\PreProtoSubprimInc{\sigma})^*\SelLiouville{\sigma}.
\]
Hence the recorded pre-proto-subprimitive Liouville one-forms and closed two-forms are exactly the shell restrictions of the deeper selection cotangent-model Liouville data.

Item (9) follows because pullback of the skew-Hermitian connection one-form along the shell inclusion gives the recorded pre-proto-subprimitive connection one-form, while parallel transport along a curve in the shell is exactly parallel transport of its included image in the deeper selection bundle. Identity on constant curves, multiplicativity under concatenation, and inversion under time reversal are inherited without change.

Item (10) is then immediate. Every visible geometric ingredient of the recorded pre-proto-subprimitive layer is recovered verbatim, but it is no longer primitive.
\end{proof}

\section{Stationary-Selection Basic Source and Fixed-Endpoint Origin of the Remaining Pre-Proto-Subprimitive Data}

\begin{definition}[Selection-basic source and fixed-endpoint action data]
\label{def:selection_basic}
Assume the stationary-selection geometry of Definition \ref{def:selection_geometry} is fixed. Assume further, for each \(\sigma\in\SectorSet\):
\begin{enumerate}
    \item there are exact-preserving selection shadow one-forms
    \[
    \SelShadowForm{\sigma}\in\Omega^1(\SelSpace{\sigma};\StatTagClass{\sigma})
    \]
    that are invariant under the corresponding selection time reversals, annihilate vectors normal to the stationary shell locus \(\PreProtoSubprimSpace{\sigma}\), annihilate vertical vectors tangent to the visible selection fibers, and are preserved along the current pre-proto-subprimitive characteristic curves singled out on the root exact-constraint shells by the restricted pre-proto-subprimitive Liouville / exact-constraint geometry. Define the recorded pre-proto-subprimitive shadow one-forms by restriction:
    \begin{equation}
    \PreProtoSubprimShadowForm{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelShadowForm{\sigma}.
    \label{eq:preproto_shadow_from_selection}
    \end{equation}
    \item there are exact-preserving selection rate one-forms
    \[
    \SelRateForm{\sigma}\in\Omega^1(\SelSpace{\sigma})
    \]
    with the same invariance and annihilation properties. Define the recorded pre-proto-subprimitive rate one-forms by
    \begin{equation}
    \PreProtoSubprimRateForm{\sigma}\equiv (\PreProtoSubprimInc{\sigma})^*\SelRateForm{\sigma}.
    \label{eq:preproto_rate_from_selection}
    \end{equation}
    \item there are exact-preserving selection density and selection flux source fields
    \[
    \SelDensitySource{\sigma}:\SelSpace{\sigma}\to\DensityClass{\sigma},
    \qquad
    \SelFluxSource{\sigma}:\SelSpace{\sigma}\to\FluxClass{\sigma},
    \]
    invariant under the corresponding selection time reversals, preserved along the current pre-proto-subprimitive characteristic curves, and constant on pre-proto-subprimitive-shell-normal directions and on vertical selection-fiber directions. Define the recorded pre-proto-subprimitive density and pre-proto-subprimitive flux source fields by restriction:
    \begin{equation}
    \PreProtoSubprimDensitySource{\sigma}\equiv \SelDensitySource{\sigma}|_{\PreProtoSubprimSpace{\sigma}},
    \qquad
    \PreProtoSubprimFluxSource{\sigma}\equiv \SelFluxSource{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_sources_from_selection}
    \end{equation}
    \item there are exact-preserving selection boundary potentials
    \[
    \SelBoundarySource{\sigma}:\SelSpace{\sigma}\to\PhaseReadClass{\sigma}
    \]
    vanishing on the corresponding selection unit sections, invariant under the corresponding selection time reversals, annihilating vectors normal to the stationary shell locus, and constant on vertical selection-fiber directions. Define the recorded pre-proto-subprimitive boundary potentials by
    \begin{equation}
    \PreProtoSubprimBoundarySource{\sigma}\equiv \SelBoundarySource{\sigma}|_{\PreProtoSubprimSpace{\sigma}}.
    \label{eq:preproto_boundary_from_selection}
    \end{equation}
    \item define the selection fixed-endpoint rate actions on \(C^2\) curves constrained to the current root exact-constraint shells inside the current primitive-generating shells, current subprimitive-generating shells, and current proto-subprimitive-generating shells by
    \begin{equation}
    \SelAction{\sigma}[\Gamma]\equiv \int_\Gamma \SelRateForm{\sigma}.
    \label{eq:selection_action}
    \end{equation}
    Assume their stationary curves at fixed visible endpoint data are exactly the current pre-proto-subprimitive characteristic curves determined on those shells by the restricted pre-proto-subprimitive Liouville / exact-constraint geometry, that their first variations vanish automatically along the characteristic, symmetry, vertical selection-fiber, and stationary-shell-normal directions, and that their second variations are positive on smooth endpoint-fixed complements to those directions and to the normal exact-constraint directions singled out by \(\ConstraintGap{\sigma}\). Define the recorded pre-proto-subprimitive fixed-endpoint actions by
    \begin{equation}
    \PreProtoSubprimAction{\sigma}[\gamma]\equiv \int_\gamma \PreProtoSubprimRateForm{\sigma}.
    \label{eq:preproto_action_from_selection}
    \end{equation}
\end{enumerate}
\end{definition}

\begin{proposition}[Pre-proto-subprimitive-basic data and pre-proto-subprimitive action are forced]
\label{prop:preproto_basic_forced}
The selection-basic data of Definition \ref{def:selection_basic} recover exactly the remaining recorded pre-proto-subprimitive layer.
\begin{enumerate}
    \item The restricted one-forms \eqref{eq:preproto_shadow_from_selection} and \eqref{eq:preproto_rate_from_selection} are exactly the recorded pre-proto-subprimitive shadow and pre-proto-subprimitive rate one-forms.
    \item The restricted source fields \eqref{eq:preproto_sources_from_selection} are exactly the recorded pre-proto-subprimitive density and pre-proto-subprimitive flux source fields.
    \item The restricted boundary potentials \eqref{eq:preproto_boundary_from_selection} are exactly the recorded pre-proto-subprimitive boundary potentials.
    \item The fixed-endpoint actions \eqref{eq:preproto_action_from_selection} are exactly the recorded pre-proto-subprimitive actions, and their inherited second-variation positivity is exactly the recorded fixed-endpoint pre-proto-subprimitive rate-action geometry.
    \item Consequently the pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data and the fixed-endpoint pre-proto-subprimitive rate-action geometry are no longer primitive at present scope; they are forced by deeper selection-basic restriction and the same fixed-endpoint stationary-selection variational geometry.
\end{enumerate}
\end{proposition}

\begin{proof}
Item (1) follows from the restriction construction itself. Because the selection shadow and selection rate one-forms annihilate both the stationary-shell-normal directions and the vertical directions tangent to the visible selection fibers, their restrictions to the pre-proto-subprimitive zero shells are well defined and already satisfy the invariance and horizontality required at pre-proto-subprimitive level.

Item (2) is immediate from the restriction formula \eqref{eq:preproto_sources_from_selection}. Constancy on pre-proto-subprimitive-shell-normal directions and on vertical selection-fiber directions guarantees that the restricted source fields depend only on the pre-proto-subprimitive states themselves, while the assumed characteristic-flow and time-reversal invariance give exactly the recorded exact-preserving behavior.

Item (3) is the same argument applied to the boundary potentials. Since the selection boundary potentials vanish on the selection unit sections, their pre-proto-subprimitive restrictions vanish on the recorded pre-proto-subprimitive unit sections as required.

Item (4) follows from fixed-endpoint stationary reduction. The selection rate actions are assumed to have precisely the current pre-proto-subprimitive characteristic curves as stationary curves, with first variations vanishing automatically along the characteristic, symmetry, vertical selection-fiber, and stationary-shell-normal directions. Restricting those actions to curves in the stationary zero shells yields exactly the recorded pre-proto-subprimitive-action formula \eqref{eq:preproto_action_from_selection}. The positive second variation on endpoint-fixed complements descends directly to the recorded pre-proto-subprimitive stationary geometry.

Item (5) is therefore immediate. None of the current pre-proto-subprimitive-basic data or pre-proto-subprimitive fixed-endpoint stationary geometry needs to be inserted separately once the selection-basic layer is fixed.
\end{proof}

\section{Exact Recovery of the Recorded Pre-Proto-Subprimitive Layer and Downstream Package}

\begin{theorem}[Same pre-proto-subprimitive input layer]
\label{thm:same_preproto_input}
Under the fixed branch of Definition \ref{def:fixed_branch}, the deeper stationary-selection / cotangent-connection / source-action construction recovers exactly the pre-proto-subprimitive input layer of Definitions \ref{def:recorded_preproto_geometry} and \ref{def:recorded_preproto_basic}.
\end{theorem}

\begin{proof}
The geometric part is Proposition \ref{prop:preproto_geometry_forced}. The remaining basic-data and fixed-endpoint-action part is Proposition \ref{prop:preproto_basic_forced}. Together they recover every recorded item without alteration.
\end{proof}

\begin{theorem}[Same proto-subprimitive layer and same downstream package]
\label{thm:same_package}
Under the fixed branch of Definition \ref{def:fixed_branch}, the deeper stationary-selection / cotangent-connection / source-action construction preserves without change:
\begin{enumerate}
    \item the same pre-proto-subprimitive stationary-selection / lifted connection-cotangent / source-action input layer;
    \item the same proto-subprimitive substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport input layer;
    \item the same subprimitive substrate / primitive-generating shell / lifted connection-cotangent / parallel-transport input layer;
    \item the same primitive substrate / root-generating shell / lifted connection-cotangent input layer;
    \item the same root-shell / lifted connection-cotangent / parallel-transport input layer;
    \item the same pre-ambient-shell / connection-cotangent / parallel-transport input layer;
    \item the same ambient-shell / cotangent-Liouville / connection-holonomy input layer;
    \item the same constrained substrate-bundle / exact-constraint / Liouville-holonomy input layer;
    \item the same proto-process / stationary-selection / characteristic-bundle input layer;
    \item the same process-state / reversible-line-field / compact-calibration-fiber / basic-form layer;
    \item the same reversible-process-thread / calibration-generator / shadow-current / rate-density / boundary-recorder layer;
    \item the same reversible-process-window / calibration-action / stationary-shadow / local-rate / endpoint-recorder layer;
    \item the same reversible-history / stationary-equivalence / short-history-action / endpoint-readout / cotangent-lift layer;
    \item the same reversible-groupoid / transport-action / constrained-stationary package;
    \item the same reconfiguration-group / transport-law / equilibrium-reduction package;
    \item the same derivation-algebra / balance-law / equilibrium-manifold layer;
    \item hence the same process-state and reversible process-thread / process-window / history / groupoid / reconfiguration / derivation chain;
    \item the same \(\StemFunctional\);
    \item the same exact-preservation normal form \(\DeepFunctional\);
    \item the same carrier-origin functional \(\CarrierFunctional[\CarrierSeed,\RelabelSeed,\FreeSeed]\);
    \item the same primitive reservoir \(\PrimFunctional[\PrimStrain,\PrimNeutral,\PrimFree]\);
    \item the same microscopic-equilibrium reservoir \(\MicFunctional[H,\GaugeAux]\);
    \item the same free-sector verdict \(\FreeProj\CompLift=0\), and hence the same \(\Pi_{\mathrm{free}}H=0\) outcome;
    \item the same substrate and observer completion solves \eqref{eq:fixed_solves};
    \item the same \(\Sigma^{\mathrm{cmp}}\) solve, the same observer-equation closure, the same one operative metric \(\CompletedMetric\), and the same recorded Phase D / E and Phase F gains.
\end{enumerate}
\end{theorem}

\begin{proof}
Theorem \ref{thm:same_preproto_input} reproduces exactly the input layer required by the immediately preceding proto-subprimitive-substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport origin note. That note already proves that once this pre-proto-subprimitive input layer is fixed, the same proto-subprimitive layer follows without change, and with it the same full downstream chain. Since none of the visible slots, elimination steps, or completion equations are altered here, every downstream object listed above is reproduced verbatim.
\end{proof}

\begin{corollary}[Recorded gain package is preserved]
\label{cor:gains}
Because the same downstream package is recovered, the recorded Phase D / E infrared-spectrum and universal-coupling verdicts, the recorded Phase F controlled GR limit, and the already recorded observer-equation closure package remain preserved without modification.
\end{corollary}

\begin{proof}
This is the direct content of Theorem \ref{thm:same_package}.
\end{proof}

\section{Claim-Boundary Verdict}

\begin{theorem}[Pre-proto-subprimitive stationary-selection origin verdict]
\label{thm:verdict}
At the disciplined scope of the present note, the exact positive result is the following.
\begin{enumerate}
    \item The exact-preserving pre-proto-subprimitive substrate bundles \(\PreProtoSubprimSpace{\Car},\PreProtoSubprimSpace{\Cur},\PreProtoSubprimSpace{\Hom}\) are no longer primitive at current scope.
    \item They arise as the exact stationary zero shells of deeper selection sections \(\SelSection{\Car},\SelSection{\Cur},\SelSection{\Hom}\) on compact visible selection bundles, equivalently as the zero-energy stationary loci of the deeper selection functionals \(\SelFunctional{\Car},\SelFunctional{\Cur},\SelFunctional{\Hom}\).
    \item The visible pre-proto-subprimitive readout maps, pre-proto-subprimitive unit sections, pre-proto-subprimitive time reversals, and preserved positive pre-proto-subprimitive metrics are no longer primitive either.
    \item They are the exact-shell restrictions of deeper selection readouts, units, time reversals, and selection metrics, and the recorded compact visible pre-proto-subprimitive fibers are closed stationary-shell slices inside compact visible selection fibers.
    \item The proto-subprimitive-generating shell sections are no longer primitive either.
    \item They arise as the pre-proto-subprimitive-shell restrictions of deeper lifted proto-subprimitive-generating shell maps into the same positive target spaces with the same target metrics \(\PreProtoSubprimTargetMetric{\Car},\PreProtoSubprimTargetMetric{\Cur},\PreProtoSubprimTargetMetric{\Hom}\).
    \item The lifted subprimitive-generating shell maps, lifted primitive-generating shell maps, lifted root-generating shell maps, lifted pre-ambient-generating shell maps, lifted ambient-generating shell maps, lifted shell maps, and lifted exact-constraint maps are no longer primitive either.
    \item They arise as the pre-proto-subprimitive-shell restrictions of deeper lifted maps into the same recorded target spaces with the same target metrics.
    \item The pre-proto-subprimitive cotangent-model Liouville one-forms \(\PreProtoSubprimLiouville{\Car},\PreProtoSubprimLiouville{\Cur},\PreProtoSubprimLiouville{\Hom}\) and the pre-proto-subprimitive closed two-form geometry \(\PreProtoSubprimTwo{\Car},\PreProtoSubprimTwo{\Cur},\PreProtoSubprimTwo{\Hom}\) are no longer primitive either.
    \item They arise as the pre-proto-subprimitive-shell restrictions of deeper selection cotangent-model Liouville one-forms \(\SelLiouville{\Car},\SelLiouville{\Cur},\SelLiouville{\Hom}\) and their exact two-forms on the selection bundles.
    \item The pre-proto-subprimitive connection one-forms \(\PreProtoSubprimConnectionForm{\Car},\PreProtoSubprimConnectionForm{\Cur},\PreProtoSubprimConnectionForm{\Hom}\) and their induced parallel transports \(\PreProtoSubprimTransport{\Car},\PreProtoSubprimTransport{\Cur},\PreProtoSubprimTransport{\Hom}\) are no longer primitive either.
    \item They arise as pre-proto-subprimitive-shell restrictions of deeper skew-Hermitian selection connection one-forms \(\SelConnectionForm{\Car},\SelConnectionForm{\Cur},\SelConnectionForm{\Hom}\) and of their induced selection parallel transports.
    \item The pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data and the fixed-endpoint pre-proto-subprimitive rate-action geometry are no longer primitive either.
    \item They arise as pre-proto-subprimitive-shell restrictions of deeper selection-basic source data and the same fixed-endpoint stationary-selection variational geometry.
    \item Therefore the full pre-proto-subprimitive layer of the immediately preceding proto-subprimitive note is recovered without change.
    \item Consequently the same proto-subprimitive layer, the same subprimitive layer, the same primitive layer, the same root layer, the same pre-ambient layer, the same ambient-shell / cotangent-Liouville / connection-holonomy layer, the same process-state and reversible-chain package, the same \(\StemFunctional\), the same exact-preservation normal form, the same carrier-origin / primitive-reservoir / microscopic-equilibrium functionals, the same \(\Sigma^{\mathrm{cmp}}\) solve, the same \(\Pi_{\mathrm{free}}H=0\) outcome, the same observer-equation closure, the same one operative metric, and the same recorded Phase D / E and Phase F gains remain unchanged.
    \item Stronger promotion language is still not justified here, because the present note explains only why the previous pre-proto-subprimitive layer arises from a deeper stationary-selection / cotangent-connection / source-action layer; it does not yet derive why that new deeper selection layer is itself unique, final, or inevitable beyond current scope.
    \item No broader packaging consequence or default side-line continuation follows from the mathematical content of this note alone. The remaining honest burden is one layer deeper again on this same exact-preservation line, or else another comparably concrete observer-equation gain on the same one-metric route.
\end{enumerate}
\end{theorem}

\begin{proof}
Items (1)--(12) are Proposition \ref{prop:preproto_geometry_forced}. Items (13) and (14) are Proposition \ref{prop:preproto_basic_forced}. Items (15) and (16) are Theorems \ref{thm:same_preproto_input} and \ref{thm:same_package} together with Corollary \ref{cor:gains}. Items (17) and (18) are the present claim-boundary discipline stated in the abstract and introduction.
\end{proof}

\begin{corollary}[What remains next]
\label{cor:what_next}
The primary active burden moves one layer deeper again. It is no longer to justify why the pre-proto-subprimitive substrate bundles, their proto-subprimitive-generating shell sections, their lifted subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps, their pre-proto-subprimitive Liouville / closed-two-form geometry, their connection one-forms and induced parallel transports, or their pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data and fixed-endpoint pre-proto-subprimitive rate-action geometry are admissible at current scope; that step is now recorded. The next honest burden is either to derive or justify why the deeper stationary-selection bundles, their exact stationary-selection sections and functionals, their lifted proto-subprimitive-generating / subprimitive-generating / primitive-generating / root-generating / pre-ambient-generating / ambient-generating / shell / exact-constraint maps, their selection cotangent-model Liouville one-forms, their selection connection one-forms and parallel transports, and their selection-basic source / action data are themselves forced by yet more primitive substrate structure, or else to produce another comparably concrete observer-equation gain on the same one-metric line. No stronger promotion language, no packaging pass, and no default move onto the anchored positive-pair side line are justified unless they directly discharge one of those burdens.
\end{corollary}

\section{Conclusion}

The positive result of this note is that the pre-proto-subprimitive layer is no longer left primitive at present scope. It is recovered from a still deeper stationary-selection / cotangent-connection / source-action layer.

At that deeper level, the recorded pre-proto-subprimitive substrate bundles
\[
\PreProtoSubprimSpace{\Car},\qquad
\PreProtoSubprimSpace{\Cur},\qquad
\PreProtoSubprimSpace{\Hom}
\]
are exact stationary zero shells inside compact visible selection bundles. Their readout maps, unit sections, time reversals, and preserved positive metrics are exact-shell restrictions of deeper selection data. Their proto-subprimitive-generating shell sections are pre-proto-subprimitive-shell restrictions of deeper lifted proto-subprimitive-generating shell maps into the same positive target spaces with the same target metrics. Their lifted subprimitive-generating shell maps, lifted primitive-generating shell maps, lifted root-generating shell maps, lifted pre-ambient-generating shell maps, lifted ambient-generating shell maps, lifted shell maps, and lifted exact-constraint maps are pre-proto-subprimitive-shell restrictions of deeper lifted maps into the same target spaces with the same target metrics. Their pre-proto-subprimitive Liouville one-forms and closed two-form geometry are pre-proto-subprimitive-shell restrictions of deeper selection cotangent-model Liouville data. Their pre-proto-subprimitive connection one-forms and induced parallel transports are pre-proto-subprimitive-shell restrictions of deeper skew-Hermitian selection connection one-forms and their selection parallel transports. Their pre-proto-subprimitive-basic shadow / rate / density / flux / boundary data and their fixed-endpoint pre-proto-subprimitive rate-action geometry are pre-proto-subprimitive-shell restrictions of deeper selection-basic data and the same fixed-endpoint stationary-selection variational geometry.

Because that deeper construction reproduces the full pre-proto-subprimitive layer without changing any of its visible slots, the immediately preceding proto-subprimitive-substrate / subprimitive-generating shell / lifted connection-cotangent / parallel-transport origin note applies verbatim. Consequently the same proto-subprimitive layer, the same subprimitive layer, the same primitive layer, the same root layer, the same pre-ambient layer, the same ambient-shell / cotangent-Liouville / connection-holonomy layer, the same process-state and reversible-chain package, the same \(\StemFunctional\), the same exact-preservation normal form, the same carrier-origin / primitive-reservoir / microscopic-equilibrium functionals, the same \(\Sigma^{\mathrm{cmp}}\) solve, the same \(\Pi_{\mathrm{free}}H=0\) outcome, the same observer-equation closure, the same one operative metric, and the same recorded Phase D / E and Phase F gains remain unchanged.

The scope is still disciplined rather than final. The present note does not prove that the deeper stationary-selection / cotangent-connection / source-action layer is unique or ultimate. The next honest burden is to derive or justify why that new deeper layer itself is forced by still more primitive substrate structure, or else to record another comparably concrete observer-equation gain on the same one-metric line.

\end{document}
