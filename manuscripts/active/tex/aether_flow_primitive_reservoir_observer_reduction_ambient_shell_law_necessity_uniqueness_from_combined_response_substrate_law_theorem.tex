\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm,mathtools}
\usepackage{bm}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{booktabs}
\usepackage{array}
\usepackage{setspace}
\usepackage{mathrsfs}
\usepackage{physics}

\geometry{margin=1in}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=blue, citecolor=black}
\setlist[enumerate]{topsep=0.4em,itemsep=0.35em}
\setlist[itemize]{topsep=0.3em,itemsep=0.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}
\setstretch{1.06}

\newcommand{\Aether}{\AE{}ther}
\newcommand{\Aflow}{\AE{}ther-flow}
\newcommand{\TheoryName}{\Aflow{} Interpretation of Relativity}
\newcommand{\TheoryProgram}{\Aether{} / \Aflow{} framework}

\newcommand{\CompletedMetric}{g^{\sharp}}

\newcommand{\Car}{\mathrm{car}}
\newcommand{\Cur}{\mathrm{cur}}
\newcommand{\Hom}{\mathrm{hom}}

\newcommand{\ForSpace}[1]{\mathcal I_{#1}^{\mathrm{for}}}
\newcommand{\PrimShell}{\mathcal S_{\mathrm{prim}}}

\newcommand{\Reduction}[1]{\mathcal R_{#1}}
\newcommand{\CurrentCarrier}[1]{\mathfrak C_{#1}^{\mathrm{cur}}}
\newcommand{\EqCarrier}[1]{\mathfrak C_{#1}^{\mathrm{eq}}}
\newcommand{\CurrentExtract}[1]{\mathscr E_{#1}^{\mathrm{cur}}}
\newcommand{\EqExtract}[1]{\mathscr E_{#1}^{\mathrm{eq}}}
\newcommand{\PrimCurrent}[1]{\mathcal J_{#1}^{\mathrm{prim}}}
\newcommand{\PrimTheta}[1]{\Theta_{#1}^{\mathrm{prim}}}

\newcommand{\MetricOut}{\mathscr G_{\mathrm{out}}}
\newcommand{\MatterOut}{\mathscr T_{\mathrm{out}}}
\newcommand{\DeepMetricOut}{\mathscr G_{\mathrm{deep}}}
\newcommand{\DeepMatterOut}{\mathscr T_{\mathrm{deep}}}

\newcommand{\VisibleAffine}[1]{\widehat X_{#1}}
\newcommand{\DataSpace}[1]{\mathcal Y_{#1}^{\mathrm{obs}}}

\newcommand{\AmbientSpace}[1]{\mathcal A_{#1}}
\newcommand{\AmbientBody}[1]{P_{#1}}
\newcommand{\AmbientRead}[1]{\Omega_{#1}}
\newcommand{\AmbientShellLaw}[1]{\Gamma_{#1}}
\newcommand{\AmbientValue}[1]{\gamma_{#1}}
\newcommand{\AmbientSym}[1]{\mathbb G_{#1}}
\newcommand{\AmbientTime}[1]{\vartheta_{#1}}
\newcommand{\ShellAffine}[1]{\mathcal A_{#1}^{\mathrm{sh}}}
\newcommand{\ShellVector}[1]{V_{#1}^{\mathrm{sh}}}
\newcommand{\HiddenSpace}[1]{N_{#1}^{\mathrm{hid}}}
\newcommand{\HiddenBody}[1]{B_{#1}^{\mathrm{hid}}}
\newcommand{\HiddenOperator}[1]{K_{#1}^{\mathrm{hid}}}
\newcommand{\ShellProj}[1]{\Pi_{#1}^{\mathrm{sh}}}
\newcommand{\HiddenProj}[1]{\Pi_{#1}^{\mathrm{hid}}}
\newcommand{\AmbientEmbed}[1]{\eta_{#1}}
\newcommand{\AmbientRef}[1]{a_{#1}^{0}}
\newcommand{\AmbientTarget}[1]{E_{#1}^{\mathrm{amb}}}

\newcommand{\ProtoSpace}[1]{\mathcal M_{#1}}
\newcommand{\ProtoBody}[1]{\mathcal P_{#1}}
\newcommand{\ResponseSpace}[1]{\mathcal R_{#1}}
\newcommand{\ResponseMap}[1]{\Xi_{#1}}
\newcommand{\ResponseData}[1]{\Xi_{#1}^{\mathrm{dat}}}
\newcommand{\ResponseShell}[1]{\Xi_{#1}^{\mathrm{sh}}}
\newcommand{\ResponseHidden}[1]{\Xi_{#1}^{\mathrm{hid}}}
\newcommand{\ResponseImage}[1]{\mathcal B_{#1}^{\mathrm{resp}}}
\newcommand{\GapSpace}[1]{H_{#1}}
\newcommand{\GapBody}[1]{D_{#1}}
\newcommand{\ResponseSym}[1]{\mathbb H_{#1}}
\newcommand{\ResponseTime}[1]{\rho_{#1}}
\newcommand{\ProtoEmbed}[1]{\zeta_{#1}}

\newcommand{\DeepSpace}[1]{\mathcal U_{#1}}
\newcommand{\ChainSelect}[1]{\mathcal S_{#1}}
\newcommand{\DeepShell}{\mathcal U_{\mathrm{tot}}}
\newcommand{\ChainSelectTriple}{\mathcal S_{\mathrm{tot}}}

\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}
\newtheorem{corollary}{Corollary}
\newtheorem{theorem}{Theorem}

\title{\textbf{The \TheoryName{}}\\[0.4em]
\large Primitive-Reservoir Observer-Reduction\\
\large Ambient Shell Law\\
\large Necessity / Uniqueness from Combined Response Substrate Law}
\author{}
\date{}

\begin{document}

\maketitle

\begin{abstract}
The current deepest benchmark-facing note on the active primitive-reservoir observer-reduction line already changed the burden once: the primitive reversible constrained substrate dynamics normal form is no longer merely available, because it is derived canonically from an ambient shell-law package with hidden-sector gap and is necessary there up to the forced translated-shell and orthogonal-hidden identifications. The present note goes one honest layer deeper again. The remaining task is no longer to justify that primitive normal form, but to justify why the ambient shell-law class itself is forced on the active line rather than becoming the new stopping point.

The new theorem does that by compressing the ambient shell-law package into a single deeper combined response substrate law. For each sector it starts from a compact convex primitive substrate body, one affine combined response map to observer data, shell response, and hidden response, a product-factorized response image, an exact primitive-shell realization on the zero-response locus, symmetry and time-reversal actions preserving the combined response image, a positively gapped hidden factor, shell solvability on fixed data fibers, and a response-level coercivity condition on data-invisible variations. From that single package one derives canonically the ambient state space and ambient body, the affine observer-data readout, the ambient shell plane, the affine shell law and shell value, the hidden-sector splitting and positive gap operator, the symmetry and time-reversal structure, the shell solvability property, and the shell-direction coercivity statement.

The theorem is stronger than another deeper existence relay. On this combined-response line the ambient shell-law package is necessary: the ambient body is the combined-response image body, the readout and shell law are the coordinate projections, the shell plane is the hidden-zero plane, the shell value is the zero response value, and the hidden sector is the hidden response factor with its given gap operator. After a canonical barycentric normalization of the shell-plane reference point, no further ambient freedom remains except the obvious factor-preserving affine identification. The downstream benchmark package stays fixed throughout: the unique selector defined by the zero-response shell locus still pulls back exactly the same one operative metric \(\CompletedMetric\) and exactly the same ordinary-matter route through that metric, with no second operative metric and no enlarged low-energy matter law. This remains a disciplined derivational gain rather than a completed final microphysical derivation of GR.
\end{abstract}

\section{Introduction}

The active derivational program of \TheoryName{} has again narrowed the remaining honest burden. The observer-reduction datum line, the defect-fiber factorization theorem, the one-metric output theorem, the chain-forcing theorem, the selector-existence theorem, the stationary-construction theorem, the explicit-package necessity / uniqueness theorem, and the later ambient-shell-law theorem have each discharged what was honestly open at their respective layers. The most recent of those notes matters because it alters what now counts as primary progress. Once the primitive reversible constrained dynamics normal form is itself forced by a deeper ambient shell-law package, another note that merely restates that ambient package would no longer count as the next front-line move.

The present note therefore addresses the burden that remained open after the ambient-shell-law theorem itself. The task is now to explain why that ambient shell-law class should hold on the active line. The positive move made here is to collapse that class into a still more primitive combined response substrate law in which the observer-data channel, shell response, and hidden response appear together as one affine response image of a compact convex primitive substrate body.

This note does not alter the benchmark package. It does not change the one operative completed metric, it does not change the ordinary matter route, it does not introduce a second operative metric, and it does not enlarge the low-energy matter law. Its gain is upstream and structural: the ambient shell-law package is shown to be a canonical and necessary output of a deeper combined response substrate law with hidden-sector gap.

\paragraph{Framework and claim boundary.}
\TheoryProgram{} denotes the broader \Aether{} / \Aflow{} framework built on the claims that the \Aether{} is the underlying four-dimensional substrate of reality, the \Aflow{} is its intrinsic ordered motion, observed three-dimensional space is the local experiential slice of that deeper substrate, S-time is the experienced order of change arising from matter, light, and the \Aflow{}, and observed expansion is the three-dimensional appearance of deeper four-dimensional ordered motion. Within that framework, \TheoryName{} denotes the exact-closure theory in which the effective gravitational dynamics are adopted to be exactly Einsteinian with universal matter coupling. In the active sequence, \emph{adoption} means use of that established relativistic dynamics without claiming substrate derivation, while \emph{derivation} is reserved for a first-principles recovery from explicit substrate variables.

The present note stays strictly inside that boundary. It does not claim a completed ultimate microphysical theory. Its narrower theorem is only that, on the active primitive-observer-reduction line, the ambient shell-law package is itself a forced and canonically normalized output of a deeper combined response substrate law.

\section{Fixed Primitive Continuation Data}

\begin{definition}[Recorded primitive continuation data]
\label{def:fixed}
Fix the following continuation data from the active primitive-reservoir observer-reduction line.
\begin{enumerate}
    \item For each sector label \(\sigma\in\{\Car,\Cur,\Hom\}\), a primitive forcing shell
    \[
    \ForSpace{\sigma},
    \]
    a visible reduction map
    \[
    \Reduction{\sigma}:\ForSpace{\sigma}\to X_{\sigma},
    \]
    and shell-level current and equilibrium carriers
    \[
    \CurrentCarrier{\sigma}:\ForSpace{\sigma}\to Z_{\sigma}^{\mathrm{cur}},
    \qquad
    \EqCarrier{\sigma}:\ForSpace{\sigma}\to Z_{\sigma}^{\mathrm{eq}}.
    \]
    \item The product shell
    \[
    \PrimShell
    \equiv
    \ForSpace{\Car}\times \ForSpace{\Cur}\times \ForSpace{\Hom}.
    \]
    \item Fixed shell extractors
    \[
    \CurrentExtract{\sigma}:Z_{\sigma}^{\mathrm{cur}}\to Y_{\sigma}^{\mathrm{cur}},
    \qquad
    \EqExtract{\sigma}:Z_{\sigma}^{\mathrm{eq}}\to Y_{\sigma}^{\mathrm{eq}},
    \]
    together with the recorded shell composites
    \begin{equation}
    \PrimCurrent{\sigma}
    =
    \CurrentExtract{\sigma}\circ \CurrentCarrier{\sigma},
    \qquad
    \PrimTheta{\sigma}
    =
    \EqExtract{\sigma}\circ \EqCarrier{\sigma}.
    \label{eq:primcomposites}
    \end{equation}
    \item The recorded metric and ordinary-matter outputs
    \[
    \MetricOut:\PrimShell\to\{\text{observer metrics}\},
    \qquad
    \MatterOut:\PrimShell\times\Psi\to\{\text{observer stress tensors}\},
    \]
    satisfying
    \begin{equation}
    \MetricOut(\mathbf w)=\CompletedMetric,
    \qquad
    \MatterOut(\mathbf w,\psi)
    =
    T_{\mu\nu}^{\mathrm{matter}}[\CompletedMetric,\psi]
    \label{eq:fixedoutputs}
    \end{equation}
    for every \(\mathbf w\in\PrimShell\) and every matter configuration \(\psi\in\Psi\).
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:fixed} is not reopened here. The primitive shell data and the benchmark one-metric / ordinary-matter outputs remain fixed continuation data. The new burden begins one layer deeper again: why the ambient shell-law package that previously forced the primitive normal form should itself be forced.
\end{remark}

\section{Target Ambient Shell-Law Package}

\begin{definition}[Ambient shell law with hidden-sector gap]
\label{def:ambient}
Fix \(\sigma\in\{\Car,\Cur,\Hom\}\). An \emph{ambient shell-law package with hidden-sector gap} for sector \(\sigma\) consists of the following data.
\begin{enumerate}
    \item A finite-dimensional Euclidean ambient state space
    \[
    \AmbientSpace{\sigma}
    \]
    with a compact convex body
    \[
    \AmbientBody{\sigma}\subset \AmbientSpace{\sigma}.
    \]
    \item A smooth embedding
    \[
    \jmath_{\sigma}:X_{\sigma}\hookrightarrow \VisibleAffine{\sigma}.
    \]
    \item An affine observer-data readout
    \[
    \AmbientRead{\sigma}:\AmbientSpace{\sigma}\to
    \VisibleAffine{\sigma}\times Z_{\sigma}^{\mathrm{cur}}\times Z_{\sigma}^{\mathrm{eq}}.
    \]
    \item An affine shell plane
    \[
    \ShellAffine{\sigma}\subset \AmbientSpace{\sigma}
    \]
    together with a distinguished reference point
    \[
    \AmbientRef{\sigma}\in \ShellAffine{\sigma}\cap \AmbientBody{\sigma}.
    \]
    Assume moreover that
    \[
    \AmbientRef{\sigma}\in
    \operatorname{int}_{\ShellAffine{\sigma}}
    \bigl(\AmbientBody{\sigma}\cap \ShellAffine{\sigma}\bigr).
    \]
    Write
    \[
    \ShellVector{\sigma}\equiv T\ShellAffine{\sigma},
    \qquad
    \HiddenSpace{\sigma}\equiv \bigl(\ShellVector{\sigma}\bigr)^{\perp},
    \]
    and let
    \[
    \ShellProj{\sigma}:\AmbientSpace{\sigma}\to \ShellVector{\sigma},
    \qquad
    \HiddenProj{\sigma}:\AmbientSpace{\sigma}\to \HiddenSpace{\sigma}
    \]
    denote the orthogonal projections.
    \item An affine shell law
    \[
    \AmbientShellLaw{\sigma}:\AmbientSpace{\sigma}\to \AmbientTarget{\sigma}
    \]
    and a distinguished shell value
    \[
    \AmbientValue{\sigma}\in \AmbientTarget{\sigma}.
    \]
    \item A compact symmetry group
    \[
    \AmbientSym{\sigma}\subset O(\AmbientSpace{\sigma})
    \]
    and an orthogonal involution
    \[
    \AmbientTime{\sigma}\in O(\AmbientSpace{\sigma})
    \]
    preserving \(\AmbientBody{\sigma}\), \(\ShellAffine{\sigma}\), \(\AmbientRef{\sigma}\), \(\AmbientRead{\sigma}\), \(\AmbientShellLaw{\sigma}\), and \(\AmbientValue{\sigma}\).
    \item Body splitting over shell and hidden directions: the map
    \begin{equation}
    a\longmapsto
    \bigl(
    \AmbientRef{\sigma}+\ShellProj{\sigma}(a-\AmbientRef{\sigma}),
    \,
    \HiddenProj{\sigma}(a-\AmbientRef{\sigma})
    \bigr)
    \label{eq:splitmap}
    \end{equation}
    is a bijection from \(\AmbientBody{\sigma}\) onto
    \[
    \bigl(\AmbientBody{\sigma}\cap \ShellAffine{\sigma}\bigr)
    \times
    \HiddenProj{\sigma}\bigl(\AmbientBody{\sigma}-\AmbientRef{\sigma}\bigr).
    \]
    Assume also that
    \[
    0\in
    \operatorname{int}
    \HiddenProj{\sigma}\bigl(\AmbientBody{\sigma}-\AmbientRef{\sigma}\bigr).
    \]
    \item Hidden invisibility of the observable shell law and data readout: for every
    \[
    s\in \AmbientBody{\sigma}\cap \ShellAffine{\sigma},
    \qquad
    n\in \HiddenProj{\sigma}\bigl(\AmbientBody{\sigma}-\AmbientRef{\sigma}\bigr),
    \]
    whenever \(s+n\in\AmbientBody{\sigma}\),
    \begin{equation}
    \AmbientRead{\sigma}(s+n)=\AmbientRead{\sigma}(s),
    \qquad
    \AmbientShellLaw{\sigma}(s+n)=\AmbientShellLaw{\sigma}(s).
    \label{eq:hiddeninvisible}
    \end{equation}
    \item Exact primitive-shell realization: the shell locus
    \[
    \AmbientBody{\sigma}\cap
    \ShellAffine{\sigma}\cap
    \AmbientShellLaw{\sigma}^{-1}(\AmbientValue{\sigma})
    \]
    is the image of a smooth embedding
    \[
    \AmbientEmbed{\sigma}:\ForSpace{\sigma}\hookrightarrow \AmbientSpace{\sigma}
    \]
    satisfying
    \begin{equation}
    \AmbientRead{\sigma}\bigl(\AmbientEmbed{\sigma}(w)\bigr)
    =
    \bigl(
    \jmath_{\sigma}(\Reduction{\sigma}(w)),
    \CurrentCarrier{\sigma}(w),
    \EqCarrier{\sigma}(w)
    \bigr)
    \label{eq:ambientcompat}
    \end{equation}
    for every \(w\in\ForSpace{\sigma}\).
    \item Shell solvability on fixed data fibers:
    \begin{equation}
    \AmbientRead{\sigma}(\AmbientBody{\sigma})
    =
    \AmbientRead{\sigma}
    \bigl(
    \AmbientBody{\sigma}\cap
    \ShellAffine{\sigma}\cap
    \AmbientShellLaw{\sigma}^{-1}(\AmbientValue{\sigma})
    \bigr).
    \label{eq:ambientsolvability}
    \end{equation}
    \item Shell-direction coercivity: there exists \(\kappa_{\sigma}^{\mathrm{sh}}>0\) such that for every
    \[
    \delta q\in \ShellVector{\sigma}
    \quad\text{with}\quad
    D\AmbientRead{\sigma}(\AmbientRef{\sigma})\,\delta q=0,
    \]
    one has
    \begin{equation}
    \bigl\|
    D\AmbientShellLaw{\sigma}(\AmbientRef{\sigma})\,\delta q
    \bigr\|^{2}
    \ge
    \kappa_{\sigma}^{\mathrm{sh}}\,
    \|\delta q\|^{2}.
    \label{eq:shellcoercive}
    \end{equation}
    \item A positive self-adjoint hidden-sector operator
    \[
    \HiddenOperator{\sigma}:\HiddenSpace{\sigma}\to\HiddenSpace{\sigma}
    \]
    with lower bound
    \begin{equation}
    \ev{\eta,\HiddenOperator{\sigma}\eta}
    \ge
    \kappa_{\sigma}^{\mathrm{hid}}\,
    \|\eta\|^{2}
    \qquad
    \text{for all }\eta\in\HiddenSpace{\sigma}
    \label{eq:hiddengap}
    \end{equation}
    for some \(\kappa_{\sigma}^{\mathrm{hid}}>0\), and such that \(\AmbientSym{\sigma}\) and \(\AmbientTime{\sigma}\) preserve \(\HiddenOperator{\sigma}\).
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:ambient} is the target package whose origin remained open after the previous note. The purpose here is not to reproduce it as another deeper assumption, but to show that it is a canonical output of still more primitive combined response data.
\end{remark}

\section{Combined Response Substrate Law}

\begin{definition}[Combined response substrate law with hidden-sector gap]
\label{def:combined}
Fix \(\sigma\in\{\Car,\Cur,\Hom\}\). A \emph{combined response substrate law with hidden-sector gap} for sector \(\sigma\) consists of the following data.
\begin{enumerate}
    \item A finite-dimensional Euclidean primitive substrate space
    \[
    \ProtoSpace{\sigma}
    \]
    with a compact convex body
    \[
    \ProtoBody{\sigma}\subset \ProtoSpace{\sigma}.
    \]
    \item An observer-data target space
    \[
    \DataSpace{\sigma}
    \equiv
    \VisibleAffine{\sigma}\times Z_{\sigma}^{\mathrm{cur}}\times Z_{\sigma}^{\mathrm{eq}},
    \]
    an ambient shell target
    \[
    \AmbientTarget{\sigma},
    \]
    a hidden response space
    \[
    \GapSpace{\sigma},
    \]
    and the product response space
    \[
    \ResponseSpace{\sigma}
    \equiv
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma}.
    \]
    \item An affine combined response map
    \[
    \ResponseMap{\sigma}
    =
    \bigl(
    \ResponseData{\sigma},
    \ResponseShell{\sigma},
    \ResponseHidden{\sigma}
    \bigr):
    \ProtoSpace{\sigma}\to \ResponseSpace{\sigma}.
    \]
    \item The response image body
    \begin{equation}
    \ResponseImage{\sigma}
    \equiv
    \ResponseMap{\sigma}(\ProtoBody{\sigma})
    \label{eq:imagebody}
    \end{equation}
    is compact and convex, and its shell slice
    \[
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\}
    \bigr)
    \]
    has nonempty relative interior in
    \[
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\}.
    \]
    \item A compact convex hidden body
    \[
    \GapBody{\sigma}\subset \GapSpace{\sigma}
    \]
    with
    \[
    0\in\operatorname{int}\GapBody{\sigma},
    \]
    such that the response image factorizes exactly as
    \begin{equation}
    \ResponseImage{\sigma}
    =
    \left\{
    (u,e,n):
    (u,e,0)\in\ResponseImage{\sigma},
    \quad
    n\in\GapBody{\sigma}
    \right\}.
    \label{eq:responsefactor}
    \end{equation}
    Equivalently, the map
    \begin{equation}
    (u,e,n)\longmapsto \bigl((u,e,0),n\bigr)
    \label{eq:responsebijection}
    \end{equation}
    is a bijection from \(\ResponseImage{\sigma}\) onto
    \[
    \Bigl(
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\}
    \bigr)
    \Bigr)
    \times \GapBody{\sigma}.
    \]
    \item A positive self-adjoint hidden-sector operator
    \[
    \HiddenOperator{\sigma}:\GapSpace{\sigma}\to\GapSpace{\sigma}
    \]
    with lower bound
    \begin{equation}
    \ev{\eta,\HiddenOperator{\sigma}\eta}
    \ge
    \kappa_{\sigma}^{\mathrm{hid}}\,
    \|\eta\|^{2}
    \qquad
    \text{for all }\eta\in\GapSpace{\sigma}
    \label{eq:combinedgap}
    \end{equation}
    for some \(\kappa_{\sigma}^{\mathrm{hid}}>0\).
    \item A compact symmetry group
    \[
    \ResponseSym{\sigma}\subset
    O(\DataSpace{\sigma})\times O(\AmbientTarget{\sigma})\times O(\GapSpace{\sigma})
    \]
    and an orthogonal involution
    \[
    \ResponseTime{\sigma}\in
    O(\DataSpace{\sigma})\times O(\AmbientTarget{\sigma})\times O(\GapSpace{\sigma})
    \]
    preserving \(\ResponseImage{\sigma}\), the hidden-zero slice
    \[
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\},
    \]
    the zero shell value \(0\in \AmbientTarget{\sigma}\), and \(\HiddenOperator{\sigma}\).
    \item A smooth embedding
    \[
    \jmath_{\sigma}:X_{\sigma}\hookrightarrow \VisibleAffine{\sigma}
    \]
    and a smooth shell realization map
    \[
    \ProtoEmbed{\sigma}:\ForSpace{\sigma}\hookrightarrow \ProtoSpace{\sigma}
    \]
    such that
    \[
    \ProtoEmbed{\sigma}(\ForSpace{\sigma})
    \subset
    \ProtoBody{\sigma}\cap
    \bigl(\ResponseShell{\sigma}\bigr)^{-1}(0)\cap
    \bigl(\ResponseHidden{\sigma}\bigr)^{-1}(0),
    \]
    the composition
    \begin{equation}
    \AmbientEmbed{\sigma}
    \equiv
    \ResponseMap{\sigma}\circ\ProtoEmbed{\sigma}
    \label{eq:ambientembeddef}
    \end{equation}
    is a diffeomorphism onto
    \[
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
    \bigr),
    \]
    and
    \begin{equation}
    \ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)
    =
    \bigl(
    \jmath_{\sigma}(\Reduction{\sigma}(w)),
    \CurrentCarrier{\sigma}(w),
    \EqCarrier{\sigma}(w)
    \bigr)
    \label{eq:combinedcompat}
    \end{equation}
    for every \(w\in\ForSpace{\sigma}\).
    \item Zero-response shell solvability on fixed data fibers:
    \begin{equation}
    \operatorname{pr}_{\DataSpace{\sigma}}\bigl(\ResponseImage{\sigma}\bigr)
    =
    \operatorname{pr}_{\DataSpace{\sigma}}
    \Bigl(
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
    \bigr)
    \Bigr).
    \label{eq:combinedsolvability}
    \end{equation}
    \item Response-kernel coercivity on the primitive shell: there exists \(\kappa_{\sigma}^{\mathrm{resp}}>0\) such that for every
    \[
    w\in\ForSpace{\sigma},
    \qquad
    \delta m\in T_{\ProtoEmbed{\sigma}(w)}\ProtoSpace{\sigma}
    \]
    satisfying
    \[
    D\ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m=0,
    \]
    one has
    \begin{equation}
    \bigl\|
    D\ResponseShell{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
    \bigr\|^{2}
    +
    \ev{
    D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m,
    \HiddenOperator{\sigma}
    D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
    }
    \ge
    \kappa_{\sigma}^{\mathrm{resp}}\,
    \|\delta m\|^{2}.
    \label{eq:combinedcoercive}
    \end{equation}
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:combined} is deeper than Definition \ref{def:ambient} in a specific and useful sense. It does not stipulate separately an ambient state body, an ambient shell plane, an ambient shell law, a hidden splitting, and an ambient readout. Instead it starts from one compact convex primitive substrate body and one affine combined response map. The ambient ingredients are to be recovered as canonical coordinate projections of the response image.
\end{remark}

\section{Canonical Ambient Shell Law}

\begin{definition}[Canonical ambient shell law induced by the combined response]
\label{def:canonical}
Assume Definition \ref{def:combined}. Define the canonical ambient shell-law package in sector \(\sigma\) as follows.
\begin{enumerate}
    \item The ambient state space is the response space:
    \[
    \AmbientSpace{\sigma}
    \equiv
    \ResponseSpace{\sigma}
    =
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma}.
    \]
    \item The ambient body is the response image body:
    \[
    \AmbientBody{\sigma}
    \equiv
    \ResponseImage{\sigma}.
    \]
    \item The ambient readout is the data projection:
    \begin{equation}
    \AmbientRead{\sigma}(u,e,n)\equiv u.
    \label{eq:ambientreaddef}
    \end{equation}
    \item The shell plane is the hidden-zero affine plane
    \begin{equation}
    \ShellAffine{\sigma}
    \equiv
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\},
    \label{eq:shellplanedef}
    \end{equation}
    the shell translation space is
    \[
    \ShellVector{\sigma}
    \equiv
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\},
    \]
    the hidden space is
    \[
    \HiddenSpace{\sigma}
    \equiv
    \{0\}\oplus\{0\}\oplus \GapSpace{\sigma},
    \]
    and the orthogonal projections are
    \begin{equation}
    \ShellProj{\sigma}(u,e,n)\equiv (u,e,0),
    \qquad
    \HiddenProj{\sigma}(u,e,n)\equiv (0,0,n).
    \label{eq:projectiondefs}
    \end{equation}
    \item The reference point is the barycenter of the shell-plane slice:
    \begin{equation}
    \AmbientRef{\sigma}
    \equiv
    \operatorname{bar}
    \bigl(
    \AmbientBody{\sigma}\cap \ShellAffine{\sigma}
    \bigr).
    \label{eq:refdef}
    \end{equation}
    \item The ambient shell law is the shell-response projection
    \begin{equation}
    \AmbientShellLaw{\sigma}(u,e,n)\equiv e,
    \qquad
    \AmbientValue{\sigma}\equiv 0.
    \label{eq:shelllawdef}
    \end{equation}
    \item The ambient symmetry group and time reversal are
    \[
    \AmbientSym{\sigma}\equiv \ResponseSym{\sigma},
    \qquad
    \AmbientTime{\sigma}\equiv \ResponseTime{\sigma}.
    \]
    \item The primitive shell embedding is exactly \eqref{eq:ambientembeddef}.
\end{enumerate}
\end{definition}

\begin{proposition}[The combined response law induces an ambient shell-law package]
\label{prop:ambient}
Assume Definitions \ref{def:combined} and \ref{def:canonical}. Then the canonical data of Definition \ref{def:canonical} satisfy every requirement of Definition \ref{def:ambient}.
\end{proposition}

\begin{proof}
Because \(\ResponseMap{\sigma}\) is affine and \(\ProtoBody{\sigma}\) is compact and convex, \(\AmbientBody{\sigma}=\ResponseImage{\sigma}\) is compact and convex. Equation \eqref{eq:ambientreaddef} is affine, and \eqref{eq:shellplanedef} is an affine plane in the Euclidean product space \(\AmbientSpace{\sigma}\). Its translation space is \(\ShellVector{\sigma}\), and its orthogonal complement is exactly \(\HiddenSpace{\sigma}\) because the ambient norm is the product Euclidean norm.

The shell-plane slice \(\AmbientBody{\sigma}\cap\ShellAffine{\sigma}\) is compact and convex and has nonempty relative interior by Definition \ref{def:combined}. Therefore its barycenter \(\AmbientRef{\sigma}\) lies in that relative interior, which gives the required interior condition.

Equation \eqref{eq:shelllawdef} defines an affine shell law with shell value \(0\). The symmetry and time-reversal requirements follow because every element of \(\ResponseSym{\sigma}\) and \(\ResponseTime{\sigma}\) preserves the response image body, the factor decomposition of
\[
\DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma},
\]
the hidden-zero plane, the zero shell value, and \(\HiddenOperator{\sigma}\). Since these actions preserve the compact convex shell-plane slice, they preserve its barycenter, so they also preserve \(\AmbientRef{\sigma}\).

The body-splitting bijection \eqref{eq:splitmap} is exactly the bijection \eqref{eq:responsebijection} written relative to the barycentric reference point, because \eqref{eq:responsefactor} says that
\[
(u,e,n)\in \AmbientBody{\sigma}
\quad\Longleftrightarrow\quad
(u,e,0)\in \AmbientBody{\sigma}
\ \text{and}\ 
n\in \GapBody{\sigma}.
\]
Therefore
\[
\HiddenProj{\sigma}\bigl(\AmbientBody{\sigma}-\AmbientRef{\sigma}\bigr)
=
\{(0,0,n):n\in\GapBody{\sigma}\},
\]
which contains \(0\) in its interior because \(0\in\operatorname{int}\GapBody{\sigma}\).

Hidden invisibility is immediate from the coordinate-projection formulas:
\[
\AmbientRead{\sigma}(u,e,n) = u,
\qquad
\AmbientShellLaw{\sigma}(u,e,n)=e.
\]
Adding a hidden vector changes only the third coordinate and therefore leaves both maps unchanged.

For exact primitive-shell realization, the image of \(\AmbientEmbed{\sigma}=\ResponseMap{\sigma}\circ\ProtoEmbed{\sigma}\) is, by Definition \ref{def:combined},
\[
\ResponseImage{\sigma}\cap
\bigl(
\DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
\bigr)
=
\AmbientBody{\sigma}\cap \ShellAffine{\sigma}\cap \AmbientShellLaw{\sigma}^{-1}(0).
\]
Using \eqref{eq:combinedcompat},
\[
\AmbientRead{\sigma}\bigl(\AmbientEmbed{\sigma}(w)\bigr)
=
\ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)
=
\bigl(
\jmath_{\sigma}(\Reduction{\sigma}(w)),
\CurrentCarrier{\sigma}(w),
\EqCarrier{\sigma}(w)
\bigr),
\]
which is exactly \eqref{eq:ambientcompat}.

Shell solvability follows from \eqref{eq:combinedsolvability} because
\[
\AmbientRead{\sigma}(\AmbientBody{\sigma})
=
\operatorname{pr}_{\DataSpace{\sigma}}\bigl(\ResponseImage{\sigma}\bigr)
\]
and
\[
\AmbientRead{\sigma}
\bigl(
\AmbientBody{\sigma}\cap
\ShellAffine{\sigma}\cap
\AmbientShellLaw{\sigma}^{-1}(0)
\bigr)
=
\operatorname{pr}_{\DataSpace{\sigma}}
\Bigl(
\ResponseImage{\sigma}\cap
\bigl(
\DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
\bigr)
\Bigr).
\]

For shell-direction coercivity, let
\[
\delta q\in \ShellVector{\sigma}
\]
satisfy
\[
D\AmbientRead{\sigma}(\AmbientRef{\sigma})\,\delta q=0.
\]
Since \(D\AmbientRead{\sigma}\) is the projection to the first factor, this implies
\[
\delta q=(0,\delta e,0)
\]
for some \(\delta e\in\AmbientTarget{\sigma}\). Then
\[
D\AmbientShellLaw{\sigma}(\AmbientRef{\sigma})\,\delta q
=
\delta e
\]
and, with the product norm,
\[
\|\delta q\|^{2}=\|\delta e\|^{2}.
\]
Hence \eqref{eq:shellcoercive} holds with \(\kappa_{\sigma}^{\mathrm{sh}}=1\).

Finally, the hidden operator of Definition \ref{def:ambient} is exactly the given \(\HiddenOperator{\sigma}\) on \(\GapSpace{\sigma}\), viewed on the hidden subspace \(\HiddenSpace{\sigma}=\{0\}\oplus\{0\}\oplus\GapSpace{\sigma}\). Equation \eqref{eq:hiddengap} is therefore exactly \eqref{eq:combinedgap}.
\end{proof}

\section{Forced Constituents of the Ambient Shell Law}

\begin{proposition}[The ambient ingredients are forced by the combined response law]
\label{prop:forced}
Assume Definition \ref{def:combined}. Then:
\begin{enumerate}
    \item the ambient state space is necessarily the product response space \(\ResponseSpace{\sigma}\);
    \item the ambient body is necessarily the response image body \(\ResponseImage{\sigma}\);
    \item the ambient readout and ambient shell law are necessarily the coordinate projections \eqref{eq:ambientreaddef} and \eqref{eq:shelllawdef};
    \item the shell plane is necessarily the hidden-zero plane \eqref{eq:shellplanedef};
    \item the hidden sector is necessarily the hidden response factor \(\GapSpace{\sigma}\) equipped with the given operator \(\HiddenOperator{\sigma}\);
    \item the only residual ambient bookkeeping freedom is the choice of an interior shell-plane reference point, and the barycentric choice \eqref{eq:refdef} removes that freedom canonically.
\end{enumerate}
\end{proposition}

\begin{proof}
The combined response law already singles out the product response space
\[
\DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma}
\]
as the space in which the jointly recorded observer data, shell response, and hidden response live. Any ambient package faithful to that law must therefore use an ambient state space affine-identifiable with that product space and must take its ambient body to be the image of the primitive substrate body under the combined response map, namely \(\ResponseImage{\sigma}\).

Once the response image is fixed, the readout and shell law can only be the coordinate projections to the first and second factors if they are to reproduce the very observer-data and shell-response components recorded by the deeper law. Likewise the hidden sector can only be the third factor together with its given gap operator. The shell plane is then forced to be the hidden-zero plane because that is exactly the locus on which the hidden response vanishes while the shell response remains visible.

The previous ambient-shell-law class allowed a freely chosen reference point on the shell-plane slice. On the present deeper line that freedom is bookkeeping only, because the shell-plane slice is already canonically determined by the response image. Choosing its barycenter therefore gives a unique normalized representative.
\end{proof}

\begin{theorem}[Ambient shell law is necessary and unique from combined response law]
\label{thm:necessity}
Fix the combined response substrate law of Definition \ref{def:combined}. Then the ambient shell-law package induced in Definition \ref{def:canonical} is necessary and unique on that deeper line in the following sense.
\begin{enumerate}
    \item every constituent of the ambient shell-law package is fixed by the combined response law: the ambient body, the affine readout, the shell plane, the shell law and shell value, the hidden splitting and hidden-sector operator, the symmetry and time-reversal actions, the shell solvability property, and the shell-direction coercivity statement;
    \item the only residual freedom left by the ambient-shell-law class itself is the inessential choice of a reference point in the shell-plane slice, and the barycentric normalization \eqref{eq:refdef} removes even that freedom canonically;
    \item consequently the ambient shell-law package is not merely available on this deeper line but necessary there, unique up to the obvious factor-preserving affine identification of the response coordinates.
\end{enumerate}
\end{theorem}

\begin{proof}
Item (1) is Proposition \ref{prop:ambient} together with Proposition \ref{prop:forced}. The combined response law fixes the response image body and the three response factors, so the ambient shell-law package is fixed once those data are recorded.

Item (2) is exactly the normalization statement in Proposition \ref{prop:forced}. Before normalization, the ambient-shell-law class permits any interior reference point on the shell-plane slice. But that slice is compact and convex and already fixed by item (1), so its barycenter gives a canonical choice preserved by all symmetries and by time reversal.

Item (3) is a restatement of items (1) and (2) in the present routing language. After barycentric normalization there is no remaining ambient freedom except the tautological factor-preserving affine identification of
\[
\DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma}.
\]
\end{proof}

\section{Unique Selector and Same One-Metric Output}

\begin{definition}[Deeper data space and sector selector]
\label{def:selector}
For \(\sigma\in\{\Car,\Cur,\Hom\}\), define the deeper data space by
\[
\DeepSpace{\sigma}
\equiv
\operatorname{pr}_{\DataSpace{\sigma}}\bigl(\ResponseImage{\sigma}\bigr).
\]
For \(u\in \DeepSpace{\sigma}\), let \(\ChainSelect{\sigma}(u)\) denote the unique point of \(\ForSpace{\sigma}\) satisfying
\begin{equation}
\AmbientEmbed{\sigma}\bigl(\ChainSelect{\sigma}(u)\bigr)=(u,0,0).
\label{eq:selectordef}
\end{equation}
\end{definition}

\begin{proposition}[The sector selector exists and is unique]
\label{prop:selector}
Assume Definition \ref{def:combined}. Then for every \(u\in \DeepSpace{\sigma}\), the point \(\ChainSelect{\sigma}(u)\) of Definition \ref{def:selector} exists and is unique.
\end{proposition}

\begin{proof}
Existence follows from the zero-response shell solvability property \eqref{eq:combinedsolvability}: if \(u\in\DeepSpace{\sigma}\), then \((u,0,0)\) lies in
\[
\ResponseImage{\sigma}\cap
\bigl(
\DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
\bigr).
\]
By Definition \ref{def:combined}, the map \(\AmbientEmbed{\sigma}\) is a diffeomorphism from \(\ForSpace{\sigma}\) onto that entire shell locus. Therefore there is a unique \(w\in\ForSpace{\sigma}\) with \(\AmbientEmbed{\sigma}(w)=(u,0,0)\), and that \(w\) is exactly \(\ChainSelect{\sigma}(u)\).
\end{proof}

\begin{definition}[Total selector and deeper outputs]
\label{def:deepoutputs}
Define
\[
\DeepShell
\equiv
\DeepSpace{\Car}\times\DeepSpace{\Cur}\times\DeepSpace{\Hom},
\]
\[
\ChainSelectTriple(u_{\Car},u_{\Cur},u_{\Hom})
\equiv
\bigl(
\ChainSelect{\Car}(u_{\Car}),
\ChainSelect{\Cur}(u_{\Cur}),
\ChainSelect{\Hom}(u_{\Hom})
\bigr).
\]
Define
\[
\DeepMetricOut:\DeepShell\to\{\text{observer metrics}\}
\]
and
\[
\DeepMatterOut:\DeepShell\times\Psi\to\{\text{observer stress tensors}\}
\]
by
\begin{equation}
\DeepMetricOut
\equiv
\MetricOut\circ\ChainSelectTriple,
\qquad
\DeepMatterOut(\mathbf u,\psi)
\equiv
\MatterOut(\ChainSelectTriple(\mathbf u),\psi).
\label{eq:deepoutputs}
\end{equation}
\end{definition}

\begin{theorem}[The combined response law preserves the same one-metric benchmark package]
\label{thm:outputs}
For every \(\mathbf u\in\DeepShell\) and every matter configuration \(\psi\in\Psi\),
\begin{equation}
\DeepMetricOut(\mathbf u)=\CompletedMetric
\label{eq:deepmetric}
\end{equation}
and
\begin{equation}
\DeepMatterOut(\mathbf u,\psi)
=
T_{\mu\nu}^{\mathrm{matter}}[\CompletedMetric,\psi].
\label{eq:deepmatter}
\end{equation}
Consequently the deeper combined response law introduces neither a second operative metric nor an enlarged low-energy matter law.
\end{theorem}

\begin{proof}
Equation \eqref{eq:deepmetric} follows immediately from \eqref{eq:deepoutputs} and \eqref{eq:fixedoutputs}:
\[
\DeepMetricOut(\mathbf u)
=
\MetricOut(\ChainSelectTriple(\mathbf u))
=
\CompletedMetric.
\]
Likewise,
\[
\DeepMatterOut(\mathbf u,\psi)
=
\MatterOut(\ChainSelectTriple(\mathbf u),\psi)
=
T_{\mu\nu}^{\mathrm{matter}}[\CompletedMetric,\psi],
\]
which is \eqref{eq:deepmatter}.
\end{proof}

\section{Main Theorem}

\begin{theorem}[Combined response substrate law forces the ambient shell law]
\label{thm:main}
Assume the fixed primitive continuation data of Definition \ref{def:fixed} and, for each sector \(\sigma\in\{\Car,\Cur,\Hom\}\), a combined response substrate law with hidden-sector gap in the sense of Definition \ref{def:combined}. Then:
\begin{enumerate}
    \item the ambient shell-law package of Definition \ref{def:ambient} is canonically induced by the deeper combined response law;
    \item the ambient body, the affine observer-data readout, the shell plane, the shell law and shell value, the hidden-sector splitting and positive hidden operator, the symmetry / time-reversal structure, the shell solvability property, and the shell-direction coercivity statement are all forced by that deeper law;
    \item after the barycentric normalization of the shell-plane reference point, the induced ambient shell-law package is unique up to the obvious factor-preserving affine identification of the response coordinates;
    \item the sectorwise selectors \(\ChainSelect{\sigma}\) and the total selector \(\ChainSelectTriple\) therefore exist uniquely;
    \item pulling the fixed primitive outputs back along that unique total selector yields exactly the same one operative metric \(\CompletedMetric\) and exactly the same ordinary-matter route through that metric;
    \item consequently the remaining burden after the previous ambient-shell-law note is now discharged in the stronger form required on the active line: the ambient shell-law package is no longer a deepest stopping point there, but a canonical and necessary output of the deeper combined response substrate law.
\end{enumerate}
\end{theorem}

\begin{proof}
Item (1) is Proposition \ref{prop:ambient}. Items (2) and (3) are Theorem \ref{thm:necessity}. Item (4) is Proposition \ref{prop:selector} together with Definition \ref{def:deepoutputs}. Item (5) is Theorem \ref{thm:outputs}. Item (6) is the combined routing consequence of the previous items.
\end{proof}

\begin{corollary}[What remains open after this theorem]
\label{cor:next}
After Theorem \ref{thm:main}, the remaining honest burden is no longer to justify the ambient shell-law class on the active primitive-observer-reduction line. That class is now forced, within the present combined-response line, by a deeper compact-convex substrate body together with a single affine combined response map and a hidden-sector gap. The next honest burden is deeper again: derive or justify that combined response substrate law itself from still more primitive substrate dynamics, or else prove a sharper inevitability theorem that removes even that remaining combined-response freedom.
\end{corollary}

\begin{proof}
Theorem \ref{thm:main} converts the ambient shell-law package from a remaining benchmark-facing burden into a canonical output of the deeper combined response law. What remains open is why that combined response law itself should hold, rather than becoming the current deepest disciplined assumption.
\end{proof}

\section{Conclusion}

The positive result of this note is again narrower than a completed final microphysical derivation and stronger than another deeper same-output relay. Starting from a compact-convex primitive substrate body equipped with one affine combined response map to observer data, shell response, and hidden response, together with a hidden-sector gap and exact zero-response shell realization, the note derives canonically the ambient shell-law package that had remained open after the previous theorem. The ambient state body, the affine readout, the ambient shell plane, the shell law and shell value, the hidden-sector splitting, the positive hidden operator, the symmetry / time-reversal structure, the shell solvability statement, and the shell-direction coercivity mechanism are no longer treated as separately inserted ambient ingredients. They are forced outputs of the deeper combined response law.

The stronger theorem is the necessity / uniqueness statement. On this deeper line, the entire ambient shell-law class collapses to the response image body and its three coordinate factors. The only residual freedom left by the older ambient presentation is the choice of shell-plane reference point, and even that disappears after barycentric normalization. The benchmark boundary remains fixed throughout: the unique selector determined by the zero-response shell locus still pulls back exactly the same one operative metric \(\CompletedMetric\) and exactly the same ordinary-matter route through that metric, with no second operative metric and no enlarged low-energy matter law.

This remains a disciplined derivational gain rather than a final completion claim. The note does not yet derive the combined response substrate law itself from ultimate substrate microphysics. Its honest remaining burden is exactly that deeper next step. But the current primary burden named by the project goal is now handled in the stronger form required there: the ambient shell-law package is no longer merely reproduced, but forced.

\end{document}
