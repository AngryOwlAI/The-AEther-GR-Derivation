\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{amsmath,amssymb,amsfonts,amsthm,mathtools}
\usepackage{bm}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{booktabs}
\usepackage{array}
\usepackage{setspace}
\usepackage{mathrsfs}
\usepackage{physics}

\geometry{margin=1in}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=blue, citecolor=black}
\setlist[enumerate]{topsep=0.4em,itemsep=0.35em}
\setlist[itemize]{topsep=0.3em,itemsep=0.25em}
\titleformat{\section}{\large\bfseries}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{0.5em}{}
\setstretch{1.06}

\newcommand{\Aether}{\AE{}ther}
\newcommand{\Aflow}{\AE{}ther-flow}
\newcommand{\TheoryName}{\Aflow{} Interpretation of Relativity}
\newcommand{\TheoryProgram}{\Aether{} / \Aflow{} framework}

\newcommand{\CompletedMetric}{g^{\sharp}}

\newcommand{\Car}{\mathrm{car}}
\newcommand{\Cur}{\mathrm{cur}}
\newcommand{\Hom}{\mathrm{hom}}

\newcommand{\ForSpace}[1]{\mathcal I_{#1}^{\mathrm{for}}}
\newcommand{\PrimShell}{\mathcal S_{\mathrm{prim}}}

\newcommand{\Reduction}[1]{\mathcal R_{#1}}
\newcommand{\CurrentCarrier}[1]{\mathfrak C_{#1}^{\mathrm{cur}}}
\newcommand{\EqCarrier}[1]{\mathfrak C_{#1}^{\mathrm{eq}}}
\newcommand{\CurrentExtract}[1]{\mathscr E_{#1}^{\mathrm{cur}}}
\newcommand{\EqExtract}[1]{\mathscr E_{#1}^{\mathrm{eq}}}
\newcommand{\PrimCurrent}[1]{\mathcal J_{#1}^{\mathrm{prim}}}
\newcommand{\PrimTheta}[1]{\Theta_{#1}^{\mathrm{prim}}}

\newcommand{\MetricOut}{\mathscr G_{\mathrm{out}}}
\newcommand{\MatterOut}{\mathscr T_{\mathrm{out}}}

\newcommand{\VisibleAffine}[1]{\widehat X_{#1}}
\newcommand{\DataSpace}[1]{\mathcal Y_{#1}^{\mathrm{obs}}}
\newcommand{\AmbientTarget}[1]{E_{#1}^{\mathrm{amb}}}

\newcommand{\ProtoSpace}[1]{\mathcal M_{#1}}
\newcommand{\ProtoBody}[1]{\mathcal P_{#1}}
\newcommand{\ResponseSpace}[1]{\mathcal R_{#1}}
\newcommand{\ResponseMap}[1]{\Xi_{#1}}
\newcommand{\ResponseData}[1]{\Xi_{#1}^{\mathrm{dat}}}
\newcommand{\ResponseShell}[1]{\Xi_{#1}^{\mathrm{sh}}}
\newcommand{\ResponseHidden}[1]{\Xi_{#1}^{\mathrm{hid}}}
\newcommand{\ResponseImage}[1]{\mathcal B_{#1}^{\mathrm{resp}}}
\newcommand{\GapSpace}[1]{H_{#1}}
\newcommand{\GapBody}[1]{D_{#1}}
\newcommand{\HiddenOperator}[1]{K_{#1}^{\mathrm{hid}}}
\newcommand{\ResponseSym}[1]{\mathbb H_{#1}}
\newcommand{\ResponseTime}[1]{\rho_{#1}}
\newcommand{\ProtoEmbed}[1]{\zeta_{#1}}

\newcommand{\GenSpace}[1]{\mathcal G_{#1}^{\mathrm{sel}}}
\newcommand{\GenBody}[1]{\mathcal B_{#1}^{\mathrm{sel}}}
\newcommand{\GenData}[1]{\Lambda_{#1}^{\mathrm{sel,dat}}}
\newcommand{\GenShell}[1]{\Lambda_{#1}^{\mathrm{sel,sh}}}
\newcommand{\HiddenSel}[1]{\Sigma_{#1}^{\mathrm{hid}}}
\newcommand{\ZeroHiddenSlice}[1]{\mathcal Z_{#1}^{0}}
\newcommand{\ZeroHiddenVec}[1]{V_{#1}^{0}}
\newcommand{\GenSym}[1]{\mathbb S_{#1}}
\newcommand{\GenTime}[1]{\vartheta_{#1}^{\mathrm{sel}}}
\newcommand{\GenEmbed}[1]{\chi_{#1}}
\newcommand{\Kernel}[1]{K_{#1}^{0}}
\newcommand{\StateComp}[1]{P_{#1}^{\mathrm{co}}}

\newcommand{\CoreState}[1]{\mathcal Q_{#1}}
\newcommand{\ReferencePoint}[1]{q_{#1}^{\ast}}
\newcommand{\CoreCoord}[1]{\pi_{#1}^{\mathrm{co}}}
\newcommand{\CoreBody}[1]{\mathcal Q_{#1}^{\mathrm{bod}}}
\newcommand{\CoreEmbed}[1]{\upsilon_{#1}}

\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}
\newtheorem{corollary}{Corollary}
\newtheorem{theorem}{Theorem}

\title{\textbf{The \TheoryName{}}\\[0.4em]
\large Primitive-Reservoir Observer-Reduction\\
\large Combined Response Substrate Law\\
\large Necessity / Uniqueness from Zero-Hidden Selector Generator Law}
\author{}
\date{}

\begin{document}

\maketitle

\begin{abstract}
The current benchmark-facing theorem on the active primitive-reservoir observer-reduction line already sharpened the burden once more: the completion-balance ambient dynamics package is no longer merely available, because it is derived canonically from a deeper combined response substrate law and is necessary there up to the obvious hidden-factor identification. The remaining honest task is therefore no longer to justify the ambient package. It is to justify why the combined response substrate law itself should hold.

The present note makes that next move in a form aligned with the live routing safeguard. For each sector it starts from a deeper zero-hidden selector generator law consisting of a compact convex selector-state body, affine observer-data and shell-response readouts, a self-adjoint idempotent hidden selector, an exact selector-level body splitting over selector-zero and hidden directions, a positively gapped hidden-sector operator on the selector image, symmetry and time-reversal actions preserving the whole selector package, an exact zero-response shell realization on the selector-zero locus, fixed-data shell solvability on that same locus, and a coercive selector-law inequality on data-invisible directions modulo the inert zero-hidden kernel.

From that selector law one derives canonically the currently live combined response substrate law itself. The primitive substrate state is the faithful quotient of the selector-zero locus by the inert zero-hidden kernel, the primitive substrate body is the quotient body crossed with the hidden selector body, the affine combined response map is the descended selector data and shell readout together with the explicit hidden coordinate, the response image factorizes exactly over the hidden body, the positive hidden-sector operator is inherited unchanged, the symmetry and time-reversal structure descend, the exact zero-response shell realization survives on the faithful quotient, fixed-data shell solvability descends, and the response-kernel coercive inequality is recovered with no remaining inert zero-hidden freedom. Within this selector-law class the combined response substrate law is not merely available. It is necessary and unique up to the harmless affine choice of faithful zero-hidden origin and the obvious orthogonal identifications on the faithful quotient and hidden factor.

The benchmark boundary remains fixed throughout. The one operative metric is still exactly \(\CompletedMetric\), the ordinary matter route is unchanged, there is no second operative metric, the low-energy matter law is not enlarged, and stronger first-principles promotion language is still not licensed. The gain is therefore disciplined and benchmark-facing: the current combined response stopping point is no longer left as an independently inserted package, but is forced by a deeper explicit selector law after quotienting out exactly the selector-zero directions already invisible to the recorded data and shell readouts.
\end{abstract}

\section{Introduction}

The active derivational program of \TheoryName{} has reached a new upstream burden. The primitive-observer-reduction datum theorem, the defect-fiber factorization theorem, the one-metric output theorem, the chain-forcing theorem, the selector theorem, the stationary-construction theorem, the explicit-package theorem, the primitive-normal-form theorem, the ambient-shell-law theorem, the combined-response theorem, the later completion-core and trace-core compressions, and the current ambient-dynamics forcing theorem have already discharged what was honestly open at their respective layers. The latest result matters because it changes what now counts as front-line progress. Once the completion-balance ambient dynamics package is itself forced by the deeper combined response substrate law, another ambient-package restatement, trace-core restatement, completion-core restatement, exact-shell affine nucleus restatement, or same-law combined-response relay no longer counts as the honest next move.

The remaining honest burden is therefore one level below the current combined-response frontier. The task is now to explain why the combined response substrate law itself should hold, rather than letting the compact primitive substrate body, the affine combined response map, the response-image factorization, the hidden-sector gap operator, the shell realization, the solvability statement, and the response-kernel coercivity mechanism stand as the new deepest disciplined assumptions. That is the burden addressed here.

The present note does not reopen the benchmark effective package. It does not alter the one operative completed metric, it does not introduce a second operative metric, and it does not enlarge the low-energy matter law. Its gain is narrower and more upstream. It shows that the current combined response substrate law is already a canonical and necessary reduction of a deeper zero-hidden selector generator law once the selector-zero directions invisible to the recorded data and shell readouts are quotiented out faithfully.

\paragraph{Framework and claim boundary.}
\TheoryProgram{} denotes the broader \Aether{} / \Aflow{} framework built on the claims that the \Aether{} is the underlying four-dimensional substrate of reality, the \Aflow{} is its intrinsic ordered motion, observed three-dimensional space is the local experiential slice of that deeper substrate, S-time is the experienced order of change arising from matter, light, and the \Aflow{}, and observed expansion is the three-dimensional appearance of deeper four-dimensional ordered motion. Within that framework, \TheoryName{} denotes the exact-closure theory in which the effective gravitational dynamics are adopted to be exactly Einsteinian with universal matter coupling. In the active sequence, \emph{adoption} means use of that established relativistic dynamics without claiming substrate derivation, while \emph{derivation} is reserved for a first-principles recovery from explicit substrate variables.

The present note stays strictly inside that boundary. It does not claim that final substrate microphysics has already been found. Its narrower theorem is only that, on the active primitive-observer-reduction line, the combined response substrate law is itself a forced and unique output of a deeper zero-hidden selector generator law after faithful inert-kernel reduction.

\section{Fixed Primitive Continuation Data}

\begin{definition}[Recorded primitive continuation data]
\label{def:fixed}
Fix the following continuation data from the active primitive-reservoir observer-reduction line.
\begin{enumerate}
    \item For each sector label \(\sigma\in\{\Car,\Cur,\Hom\}\), a primitive forcing shell
    \[
    \ForSpace{\sigma},
    \]
    a visible reduction map
    \[
    \Reduction{\sigma}:\ForSpace{\sigma}\to X_{\sigma},
    \]
    and shell-level current and equilibrium carriers
    \[
    \CurrentCarrier{\sigma}:\ForSpace{\sigma}\to Z_{\sigma}^{\mathrm{cur}},
    \qquad
    \EqCarrier{\sigma}:\ForSpace{\sigma}\to Z_{\sigma}^{\mathrm{eq}}.
    \]
    \item The product shell
    \[
    \PrimShell
    \equiv
    \ForSpace{\Car}\times \ForSpace{\Cur}\times \ForSpace{\Hom}.
    \]
    \item Fixed shell extractors
    \[
    \CurrentExtract{\sigma}:Z_{\sigma}^{\mathrm{cur}}\to Y_{\sigma}^{\mathrm{cur}},
    \qquad
    \EqExtract{\sigma}:Z_{\sigma}^{\mathrm{eq}}\to Y_{\sigma}^{\mathrm{eq}},
    \]
    together with the recorded shell composites
    \begin{equation}
    \PrimCurrent{\sigma}
    =
    \CurrentExtract{\sigma}\circ \CurrentCarrier{\sigma},
    \qquad
    \PrimTheta{\sigma}
    =
    \EqExtract{\sigma}\circ \EqCarrier{\sigma}.
    \label{eq:primcomposites}
    \end{equation}
    \item The recorded metric and ordinary-matter outputs
    \[
    \MetricOut:\PrimShell\to\{\text{observer metrics}\},
    \qquad
    \MatterOut:\PrimShell\times\Psi\to\{\text{observer stress tensors}\},
    \]
    satisfying
    \begin{equation}
    \MetricOut(\mathbf w)=\CompletedMetric,
    \qquad
    \MatterOut(\mathbf w,\psi)
    =
    T_{\mu\nu}^{\mathrm{matter}}[\CompletedMetric,\psi]
    \label{eq:fixedoutputs}
    \end{equation}
    for every \(\mathbf w\in\PrimShell\) and every matter configuration \(\psi\in\Psi\).
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:fixed} is not reopened here. The primitive shell data and the benchmark one-metric / ordinary-matter outputs remain fixed continuation data. The new burden begins one layer deeper again: why the combined response substrate law that now carries the active line should itself be forced.
\end{remark}

\section{Target Combined Response Law}

\begin{definition}[Combined response substrate law with hidden-sector gap]
\label{def:combined}
Fix \(\sigma\in\{\Car,\Cur,\Hom\}\). A \emph{combined response substrate law with hidden-sector gap} for sector \(\sigma\) consists of the following data.
\begin{enumerate}
    \item A finite-dimensional Euclidean primitive substrate space
    \[
    \ProtoSpace{\sigma}
    \]
    with a compact convex body
    \[
    \ProtoBody{\sigma}\subset \ProtoSpace{\sigma}.
    \]
    \item An observer-data target space
    \[
    \DataSpace{\sigma}
    \equiv
    \VisibleAffine{\sigma}\times Z_{\sigma}^{\mathrm{cur}}\times Z_{\sigma}^{\mathrm{eq}},
    \]
    an ambient shell target
    \[
    \AmbientTarget{\sigma},
    \]
    a hidden response space
    \[
    \GapSpace{\sigma},
    \]
    and the product response space
    \[
    \ResponseSpace{\sigma}
    \equiv
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus \GapSpace{\sigma}.
    \]
    \item An affine combined response map
    \[
    \ResponseMap{\sigma}
    =
    \bigl(
    \ResponseData{\sigma},
    \ResponseShell{\sigma},
    \ResponseHidden{\sigma}
    \bigr):
    \ProtoSpace{\sigma}\to \ResponseSpace{\sigma}.
    \]
    \item The response image body
    \begin{equation}
    \ResponseImage{\sigma}
    \equiv
    \ResponseMap{\sigma}(\ProtoBody{\sigma})
    \label{eq:imagebody}
    \end{equation}
    is compact and convex, and its shell slice
    \[
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\}
    \bigr)
    \]
    has nonempty relative interior in
    \[
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\}.
    \]
    \item A compact convex hidden body
    \[
    \GapBody{\sigma}\subset \GapSpace{\sigma}
    \]
    with
    \[
    0\in\operatorname{int}\GapBody{\sigma},
    \]
    such that the response image factorizes exactly as
    \begin{equation}
    \ResponseImage{\sigma}
    =
    \left\{
    (u,e,n):
    (u,e,0)\in\ResponseImage{\sigma},
    \quad
    n\in\GapBody{\sigma}
    \right\}.
    \label{eq:responsefactor}
    \end{equation}
    \item A positive self-adjoint hidden-sector operator
    \[
    \HiddenOperator{\sigma}:\GapSpace{\sigma}\to\GapSpace{\sigma}
    \]
    with lower bound
    \begin{equation}
    \ev{\eta,\HiddenOperator{\sigma}\eta}
    \ge
    \kappa_{\sigma}^{\mathrm{hid}}\,
    \|\eta\|^{2}
    \qquad
    \text{for all }\eta\in\GapSpace{\sigma}
    \label{eq:combinedgap}
    \end{equation}
    for some \(\kappa_{\sigma}^{\mathrm{hid}}>0\).
    \item A compact symmetry group
    \[
    \ResponseSym{\sigma}\subset
    O(\DataSpace{\sigma})\times O(\AmbientTarget{\sigma})\times O(\GapSpace{\sigma})
    \]
    and an orthogonal involution
    \[
    \ResponseTime{\sigma}\in
    O(\DataSpace{\sigma})\times O(\AmbientTarget{\sigma})\times O(\GapSpace{\sigma})
    \]
    preserving \(\ResponseImage{\sigma}\), the hidden-zero slice
    \[
    \DataSpace{\sigma}\oplus \AmbientTarget{\sigma}\oplus\{0\},
    \]
    the zero shell value \(0\in \AmbientTarget{\sigma}\), and \(\HiddenOperator{\sigma}\).
    \item A smooth embedding
    \[
    \jmath_{\sigma}:X_{\sigma}\hookrightarrow \VisibleAffine{\sigma}
    \]
    and a smooth shell realization map
    \[
    \ProtoEmbed{\sigma}:\ForSpace{\sigma}\hookrightarrow \ProtoSpace{\sigma}
    \]
    such that
    \[
    \ProtoEmbed{\sigma}(\ForSpace{\sigma})
    \subset
    \ProtoBody{\sigma}\cap
    \bigl(\ResponseShell{\sigma}\bigr)^{-1}(0)\cap
    \bigl(\ResponseHidden{\sigma}\bigr)^{-1}(0),
    \]
    the map \(\ResponseMap{\sigma}\circ\ProtoEmbed{\sigma}\) is a diffeomorphism onto
    \[
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
    \bigr),
    \]
    and
    \begin{equation}
    \ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)
    =
    \bigl(
    \jmath_{\sigma}(\Reduction{\sigma}(w)),
    \CurrentCarrier{\sigma}(w),
    \EqCarrier{\sigma}(w)
    \bigr)
    \label{eq:combinedcompat}
    \end{equation}
    for every \(w\in\ForSpace{\sigma}\).
    \item Zero-response shell solvability on fixed data fibers:
    \begin{equation}
    \operatorname{pr}_{\DataSpace{\sigma}}\bigl(\ResponseImage{\sigma}\bigr)
    =
    \operatorname{pr}_{\DataSpace{\sigma}}
    \Bigl(
    \ResponseImage{\sigma}\cap
    \bigl(
    \DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
    \bigr)
    \Bigr).
    \label{eq:combinedsolvability}
    \end{equation}
    \item Response-kernel coercivity on the primitive shell: there exists \(\kappa_{\sigma}^{\mathrm{resp}}>0\) such that for every
    \[
    w\in\ForSpace{\sigma},
    \qquad
    \delta m\in T_{\ProtoEmbed{\sigma}(w)}\ProtoSpace{\sigma}
    \]
    satisfying
    \[
    D\ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m=0,
    \]
    one has
    \begin{equation}
    \bigl\|
    D\ResponseShell{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
    \bigr\|^{2}
    +
    \ev{
    D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m,
    \HiddenOperator{\sigma}
    D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
    }
    \ge
    \kappa_{\sigma}^{\mathrm{resp}}\,
    \|\delta m\|^{2}.
    \label{eq:combinedcoercive}
    \end{equation}
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:combined} is the target package whose origin is now at issue. The purpose here is not to restate it as another deeper stopping point, but to show that it is already a canonical output of a deeper zero-hidden selector generator law after faithful inert-kernel reduction.
\end{remark}

\section{Zero-Hidden Selector Generator Law}

\begin{definition}[Zero-hidden selector generator law]
\label{def:generator}
Fix \(\sigma\in\{\Car,\Cur,\Hom\}\). A \emph{zero-hidden selector generator law} for sector \(\sigma\) consists of the following data.
\begin{enumerate}
    \item A finite-dimensional Euclidean selector-state space
    \[
    \GenSpace{\sigma},
    \]
    the observer-data space
    \[
    \DataSpace{\sigma}
    \equiv
    \VisibleAffine{\sigma}\times Z_{\sigma}^{\mathrm{cur}}\times Z_{\sigma}^{\mathrm{eq}},
    \]
    an ambient shell-response space
    \[
    \AmbientTarget{\sigma},
    \]
    and a compact convex selector-state body
    \[
    \GenBody{\sigma}\subset\GenSpace{\sigma}
    \]
    with nonempty interior.
    \item An affine observer-data readout
    \[
    \GenData{\sigma}:\GenSpace{\sigma}\to\DataSpace{\sigma}
    \]
    and an affine shell-response readout
    \[
    \GenShell{\sigma}:\GenSpace{\sigma}\to\AmbientTarget{\sigma}.
    \]
    \item A self-adjoint idempotent hidden selector
    \[
    \HiddenSel{\sigma}:\GenSpace{\sigma}\to\GenSpace{\sigma},
    \qquad
    {\HiddenSel{\sigma}}^{2}=\HiddenSel{\sigma},
    \qquad
    {\HiddenSel{\sigma}}^{*}=\HiddenSel{\sigma}.
    \]
    Define the selector-zero locus
    \[
    \ZeroHiddenSlice{\sigma}
    \equiv
    \ker\HiddenSel{\sigma},
    \]
    its translation space
    \[
    \ZeroHiddenVec{\sigma}
    \equiv
    \ker\HiddenSel{\sigma},
    \]
    and the hidden selector image
    \[
    \GapSpace{\sigma}
    \equiv
    \operatorname{im}\HiddenSel{\sigma}.
    \]
    \item A compact convex hidden body
    \[
    \GapBody{\sigma}\subset\GapSpace{\sigma}
    \]
    with
    \[
    0\in\operatorname{int}\GapBody{\sigma},
    \]
    such that the selector-state body splits exactly as
    \begin{equation}
    \GenBody{\sigma}
    =
    \left\{
    z+\eta:
    z\in\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma},
    \quad
    \eta\in\GapBody{\sigma}
    \right\}.
    \label{eq:gensplitting}
    \end{equation}
    \item A positive self-adjoint hidden-sector operator
    \[
    \HiddenOperator{\sigma}:\GapSpace{\sigma}\to\GapSpace{\sigma}
    \]
    satisfying
    \begin{equation}
    \ev{\eta,\HiddenOperator{\sigma}\eta}
    \ge
    \kappa_{\sigma}^{\mathrm{hid}}\,
    \|\eta\|^{2}
    \qquad
    \text{for all }\eta\in\GapSpace{\sigma}
    \label{eq:gengap}
    \end{equation}
    for some \(\kappa_{\sigma}^{\mathrm{hid}}>0\).
    \item A compact symmetry group
    \[
    \GenSym{\sigma}\subset
    O(\GenSpace{\sigma})\times
    O(\DataSpace{\sigma})\times
    O(\AmbientTarget{\sigma})
    \]
    and an orthogonal involution
    \[
    \GenTime{\sigma}\in
    O(\GenSpace{\sigma})\times
    O(\DataSpace{\sigma})\times
    O(\AmbientTarget{\sigma})
    \]
    preserving \(\GenBody{\sigma}\), \(\GenData{\sigma}\), \(\GenShell{\sigma}\), \(\HiddenSel{\sigma}\), \(\GapBody{\sigma}\), and \(\HiddenOperator{\sigma}\).
    \item A smooth embedding
    \[
    \jmath_{\sigma}:X_{\sigma}\hookrightarrow\VisibleAffine{\sigma}
    \]
    and a smooth zero-response shell realization
    \[
    \GenEmbed{\sigma}:\ForSpace{\sigma}\hookrightarrow\GenSpace{\sigma}
    \]
    whose image is exactly
    \[
    \GenBody{\sigma}\cap
    \ZeroHiddenSlice{\sigma}\cap
    \bigl(\GenShell{\sigma}\bigr)^{-1}(0),
    \]
    and such that
    \begin{equation}
    \GenData{\sigma}\bigl(\GenEmbed{\sigma}(w)\bigr)
    =
    \bigl(
    \jmath_{\sigma}(\Reduction{\sigma}(w)),
    \CurrentCarrier{\sigma}(w),
    \EqCarrier{\sigma}(w)
    \bigr)
    \label{eq:gencompat}
    \end{equation}
    for every \(w\in\ForSpace{\sigma}\).
    \item Fixed-data shell solvability on the selector-zero locus:
    \begin{equation}
    \GenData{\sigma}\bigl(
    \GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}
    \bigr)
    =
    \GenData{\sigma}\Bigl(
    \GenBody{\sigma}\cap
    \ZeroHiddenSlice{\sigma}\cap
    \bigl(\GenShell{\sigma}\bigr)^{-1}(0)
    \Bigr).
    \label{eq:gensolvability}
    \end{equation}
    \item The inert zero-hidden kernel
    \[
    \Kernel{\sigma}
    \equiv
    \ker\bigl(D\GenData{\sigma}\big|_{\ZeroHiddenVec{\sigma}}\bigr)
    \cap
    \ker\bigl(D\GenShell{\sigma}\big|_{\ZeroHiddenVec{\sigma}}\bigr)
    \]
    and the orthogonal projector
    \[
    \StateComp{\sigma}:
    \ZeroHiddenVec{\sigma}\to
    {\Kernel{\sigma}}^{\perp}\cap\ZeroHiddenVec{\sigma}.
    \]
    \item Selector-law coercivity on data-invisible directions: there exists \(\kappa_{\sigma}^{\mathrm{sel}}>0\) such that for every
    \[
    w\in\ForSpace{\sigma},
    \qquad
    \delta g\in T_{\GenEmbed{\sigma}(w)}\GenSpace{\sigma}
    \]
    satisfying
    \[
    D\GenData{\sigma}\,\delta g=0,
    \]
    one has
    \begin{equation}
    \|D\GenShell{\sigma}\,\delta g\|^{2}
    +
    \ev{
    \HiddenSel{\sigma}\delta g,
    \HiddenOperator{\sigma}\HiddenSel{\sigma}\delta g
    }
    \ge
    \kappa_{\sigma}^{\mathrm{sel}}
    \left(
    \|\StateComp{\sigma}( (I-\HiddenSel{\sigma})\delta g )\|^{2}
    +
    \|\HiddenSel{\sigma}\delta g\|^{2}
    \right).
    \label{eq:gencoercive}
    \end{equation}
\end{enumerate}
\end{definition}

\begin{remark}
Definition \ref{def:generator} is deeper than the current combined response package in exactly the way now needed. It still carries a selector-zero locus with an inert zero-hidden kernel. The combined response law must therefore be recovered as the faithful zero-hidden quotient together with the explicit hidden response factor.
\end{remark}

\section{Canonical Combined Response Law Induced by the Selector Law}

\begin{definition}[Canonical combined response law induced by the selector law]
\label{def:canonical}
Assume Definition \ref{def:generator}. Choose once and for all a reference shell point
\[
w_{\sigma}^{\ast}\in\ForSpace{\sigma},
\qquad
\ReferencePoint{\sigma}\equiv \GenEmbed{\sigma}(w_{\sigma}^{\ast}).
\]
Define the canonical combined response substrate law as follows.
\begin{enumerate}
    \item The faithful zero-hidden quotient space is
    \[
    \CoreState{\sigma}
    \equiv
    {\Kernel{\sigma}}^{\perp}\cap\ZeroHiddenVec{\sigma}.
    \]
    \item The faithful zero-hidden coordinate is the affine map
    \begin{equation}
    \CoreCoord{\sigma}(z)
    \equiv
    \StateComp{\sigma}\bigl(z-\ReferencePoint{\sigma}\bigr),
    \qquad
    z\in\ZeroHiddenSlice{\sigma}.
    \label{eq:corecoord}
    \end{equation}
    \item The zero-hidden quotient body is
    \begin{equation}
    \CoreBody{\sigma}
    \equiv
    \left\{
    \CoreCoord{\sigma}(z):
    z\in\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}
    \right\}.
    \label{eq:corebody}
    \end{equation}
    \item The primitive substrate space is
    \begin{equation}
    \ProtoSpace{\sigma}
    \equiv
    \CoreState{\sigma}\oplus\GapSpace{\sigma},
    \label{eq:protospace}
    \end{equation}
    and the primitive substrate body is the product body
    \begin{equation}
    \ProtoBody{\sigma}
    \equiv
    \CoreBody{\sigma}\times\GapBody{\sigma}.
    \label{eq:protobody}
    \end{equation}
    \item The affine combined response map is
    \begin{equation}
    \ResponseMap{\sigma}(m,\eta)
    \equiv
    \bigl(
    \GenData{\sigma}(\ReferencePoint{\sigma}+m),
    \GenShell{\sigma}(\ReferencePoint{\sigma}+m),
    \eta
    \bigr)
    \label{eq:responsemap}
    \end{equation}
    for \((m,\eta)\in\ProtoSpace{\sigma}\), with components
    \[
    \ResponseData{\sigma}(m,\eta)
    \equiv
    \GenData{\sigma}(\ReferencePoint{\sigma}+m),
    \qquad
    \ResponseShell{\sigma}(m,\eta)
    \equiv
    \GenShell{\sigma}(\ReferencePoint{\sigma}+m),
    \qquad
    \ResponseHidden{\sigma}(m,\eta)
    \equiv
    \eta.
    \]
    \item The compact symmetry group and time reversal are the descended selector symmetries:
    \[
    \ResponseSym{\sigma}
    \equiv
    \left\{
    (g_{\mathrm{dat}},g_{\mathrm{sh}},g_{\mathrm{hid}}):
    (g_{\mathrm{sel}},g_{\mathrm{dat}},g_{\mathrm{sh}})
    \in\GenSym{\sigma}
    \right\},
    \]
    where \(g_{\mathrm{hid}}\) is the restriction of \(g_{\mathrm{sel}}\) to \(\GapSpace{\sigma}\), and
    \[
    \ResponseTime{\sigma}
    \equiv
    \bigl(
    \GenTime{\sigma}\big|_{\DataSpace{\sigma}},
    \GenTime{\sigma}\big|_{\AmbientTarget{\sigma}},
    \GenTime{\sigma}\big|_{\GapSpace{\sigma}}
    \bigr).
    \]
    \item The exact shell realization map is
    \begin{equation}
    \ProtoEmbed{\sigma}(w)
    \equiv
    \bigl(
    \CoreCoord{\sigma}(\GenEmbed{\sigma}(w)),
    0
    \bigr),
    \qquad
    w\in\ForSpace{\sigma}.
    \label{eq:protoembed}
    \end{equation}
\end{enumerate}
\end{definition}

\begin{remark}
The choice of reference shell point \(\ReferencePoint{\sigma}\) fixes only the affine origin of the faithful zero-hidden coordinate. Changing it translates the quotient coordinate on the same faithful zero-hidden quotient and does not alter any benchmark-relevant constituent of the induced combined response law.
\end{remark}

\begin{proposition}[The selector law induces a combined response substrate law]
\label{prop:combined}
Assume Definitions \ref{def:generator}, \ref{def:combined}, and \ref{def:canonical}. Then the canonical data of Definition \ref{def:canonical} satisfy every requirement of Definition \ref{def:combined}.
\end{proposition}

\begin{proof}
Because \(\Kernel{\sigma}\subset\ZeroHiddenVec{\sigma}\) and \(\CoreState{\sigma}={\Kernel{\sigma}}^{\perp}\cap\ZeroHiddenVec{\sigma}\), every \(z\in\ZeroHiddenSlice{\sigma}\) can be written uniquely as
\[
z=\ReferencePoint{\sigma}+m+k
\qquad
\text{with }
m\in\CoreState{\sigma},
\quad
k\in\Kernel{\sigma}.
\]
Since \(\GenData{\sigma}\) and \(\GenShell{\sigma}\) are affine and \(\Kernel{\sigma}\) lies in the kernels of their linear parts on \(\ZeroHiddenVec{\sigma}\), the definitions in \eqref{eq:responsemap} are well defined and affine on \(\CoreState{\sigma}\).

The set \(\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}\) is compact and convex, so its image \(\CoreBody{\sigma}\) under the affine map \(\CoreCoord{\sigma}\) is compact and convex. Because \(\GenBody{\sigma}\) has nonempty interior and splits exactly as in \eqref{eq:gensplitting} with \(0\in\operatorname{int}\GapBody{\sigma}\), the selector-zero slice carries nonempty relative interior in \(\GenBody{\sigma}\). Therefore \(\CoreBody{\sigma}\) has nonempty interior in \(\CoreState{\sigma}\). It follows that the product body \(\ProtoBody{\sigma}=\CoreBody{\sigma}\times\GapBody{\sigma}\) is compact, convex, and has nonempty interior in \(\ProtoSpace{\sigma}=\CoreState{\sigma}\oplus\GapSpace{\sigma}\).

The response image is
\[
\ResponseImage{\sigma}
=
\left\{
\bigl(
\GenData{\sigma}(\ReferencePoint{\sigma}+m),
\GenShell{\sigma}(\ReferencePoint{\sigma}+m),
\eta
\bigr):
m\in\CoreBody{\sigma},
\ \eta\in\GapBody{\sigma}
\right\},
\]
so it is compact and convex. Its shell slice is
\[
\left\{
\bigl(
\GenData{\sigma}(\ReferencePoint{\sigma}+m),
\GenShell{\sigma}(\ReferencePoint{\sigma}+m),
0
\bigr):
m\in\CoreBody{\sigma}
\right\},
\]
which has nonempty relative interior because \(\CoreBody{\sigma}\) does. The exact response-image factorization \eqref{eq:responsefactor} holds by construction from the product body \eqref{eq:protobody}.

Equation \eqref{eq:combinedgap} is exactly \eqref{eq:gengap}. The descended symmetry and time-reversal data preserve \(\ResponseImage{\sigma}\), the hidden-zero slice, the zero shell value, and \(\HiddenOperator{\sigma}\) because the selector symmetries preserve \(\GenData{\sigma}\), \(\GenShell{\sigma}\), \(\HiddenSel{\sigma}\), \(\GapBody{\sigma}\), and \(\HiddenOperator{\sigma}\).

For the shell realization, \(\ProtoEmbed{\sigma}(\ForSpace{\sigma})\subset\ProtoBody{\sigma}\) because \(\GenEmbed{\sigma}(\ForSpace{\sigma})\subset\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}\). Also
\[
\ResponseShell{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)
=
\GenShell{\sigma}\bigl(\GenEmbed{\sigma}(w)\bigr)
=
0,
\qquad
\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)=0.
\]
Conversely, if
\[
(m,0)\in
\ProtoBody{\sigma}\cap
\bigl(\ResponseShell{\sigma}\bigr)^{-1}(0)\cap
\bigl(\ResponseHidden{\sigma}\bigr)^{-1}(0),
\]
then there exists
\[
z\in\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}
\]
with \(m=\CoreCoord{\sigma}(z)\). Since
\[
\ResponseShell{\sigma}(m,0)
=
\GenShell{\sigma}(z)
=
0,
\]
we have
\[
z\in
\GenBody{\sigma}\cap
\ZeroHiddenSlice{\sigma}\cap
\bigl(\GenShell{\sigma}\bigr)^{-1}(0).
\]
Because \(\GenEmbed{\sigma}\) is a diffeomorphism onto that exact selector-zero shell-zero locus, there is a unique \(w\in\ForSpace{\sigma}\) with \(z=\GenEmbed{\sigma}(w)\), and therefore \((m,0)=\ProtoEmbed{\sigma}(w)\). Hence \(\ProtoEmbed{\sigma}\) has exactly the required image.

Now consider the map
\[
\Phi_{\sigma}:
\CoreBody{\sigma}\cap
\bigl(\ResponseShell{\sigma}\bigr)^{-1}(0)
\to
\ResponseImage{\sigma}\cap
\bigl(
\DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
\bigr),
\qquad
\Phi_{\sigma}(m)
\equiv
\bigl(
\ResponseData{\sigma}(m,0),
0,
0
\bigr).
\]
This map is surjective by construction. It is injective because if
\[
\Phi_{\sigma}(m_{1})=\Phi_{\sigma}(m_{2}),
\]
then
\[
\ResponseData{\sigma}(m_{1},0)=\ResponseData{\sigma}(m_{2},0),
\qquad
\ResponseShell{\sigma}(m_{1},0)=\ResponseShell{\sigma}(m_{2},0)=0.
\]
Therefore \(m_{1}-m_{2}\in\CoreState{\sigma}\) lies in the kernels of the linear parts of both descended affine maps, hence in
\[
\ker\bigl(D\GenData{\sigma}\big|_{\ZeroHiddenVec{\sigma}}\bigr)
\cap
\ker\bigl(D\GenShell{\sigma}\big|_{\ZeroHiddenVec{\sigma}}\bigr)
\cap
\CoreState{\sigma}
=
\Kernel{\sigma}\cap{\Kernel{\sigma}}^{\perp}
=
\{0\}.
\]
So \(m_{1}=m_{2}\), and \(\Phi_{\sigma}\) is a diffeomorphism. Since \(\ProtoEmbed{\sigma}\) is a diffeomorphism onto the shell-zero hidden-zero slice, the composition \(\ResponseMap{\sigma}\circ\ProtoEmbed{\sigma}\) is a diffeomorphism onto
\[
\ResponseImage{\sigma}\cap
\bigl(
\DataSpace{\sigma}\oplus\{0\}\oplus\{0\}
\bigr).
\]

For the data compatibility \eqref{eq:combinedcompat}, the vectors
\[
\GenEmbed{\sigma}(w)-\ReferencePoint{\sigma}-\CoreCoord{\sigma}\bigl(\GenEmbed{\sigma}(w)\bigr)
\]
lie in \(\Kernel{\sigma}\), so they are invisible to the affine readouts on \(\ZeroHiddenVec{\sigma}\). Hence
\[
\ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)
=
\GenData{\sigma}\bigl(\GenEmbed{\sigma}(w)\bigr)
=
\bigl(
\jmath_{\sigma}(\Reduction{\sigma}(w)),
\CurrentCarrier{\sigma}(w),
\EqCarrier{\sigma}(w)
\bigr).
\]

For fixed-data solvability, let
\[
u\in\operatorname{pr}_{\DataSpace{\sigma}}\bigl(\ResponseImage{\sigma}\bigr).
\]
Then \(u=\GenData{\sigma}(z)\) for some
\[
z\in\GenBody{\sigma}\cap\ZeroHiddenSlice{\sigma}.
\]
By \eqref{eq:gensolvability}, there exists
\[
z_{0}\in
\GenBody{\sigma}\cap
\ZeroHiddenSlice{\sigma}\cap
\bigl(\GenShell{\sigma}\bigr)^{-1}(0)
\]
with \(\GenData{\sigma}(z_{0})=u\). Setting
\[
m_{0}\equiv\CoreCoord{\sigma}(z_{0})
\]
gives
\[
(m_{0},0)\in
\ProtoBody{\sigma}\cap
\bigl(\ResponseShell{\sigma}\bigr)^{-1}(0)\cap
\bigl(\ResponseHidden{\sigma}\bigr)^{-1}(0)
\]
and
\[
\ResponseData{\sigma}(m_{0},0)=u,
\]
which proves \eqref{eq:combinedsolvability}.

Finally, let \(w\in\ForSpace{\sigma}\) and let
\[
\delta m=(\delta q,\delta\eta)\in T_{\ProtoEmbed{\sigma}(w)}\ProtoSpace{\sigma}
=
\CoreState{\sigma}\oplus\GapSpace{\sigma}
\]
satisfy
\[
D\ResponseData{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m=0.
\]
Then
\[
D\GenData{\sigma}\,\delta q=0
\]
with \(\delta q\in\CoreState{\sigma}\subset\ZeroHiddenVec{\sigma}\). Applying \eqref{eq:gencoercive} to \(\delta g=\delta q\) gives
\[
\|D\GenShell{\sigma}\,\delta q\|^{2}
\ge
\kappa_{\sigma}^{\mathrm{sel}}\,
\|\delta q\|^{2},
\]
because \(\HiddenSel{\sigma}\delta q=0\) and \(\StateComp{\sigma}\delta q=\delta q\). Therefore
\begin{align*}
\bigl\|
D\ResponseShell{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
\bigr\|^{2}
+
\ev{
D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m,
\HiddenOperator{\sigma}
D\ResponseHidden{\sigma}\bigl(\ProtoEmbed{\sigma}(w)\bigr)\,\delta m
}
&=
\|D\GenShell{\sigma}\,\delta q\|^{2}
+
\ev{\delta\eta,\HiddenOperator{\sigma}\delta\eta}
\\
&\ge
\kappa_{\sigma}^{\mathrm{sel}}\|\delta q\|^{2}
+
\kappa_{\sigma}^{\mathrm{hid}}\|\delta\eta\|^{2}
\\
&\ge
\kappa_{\sigma}^{\mathrm{resp}}
\bigl(
\|\delta q\|^{2}+\|\delta\eta\|^{2}
\bigr),
\end{align*}
where
\[
\kappa_{\sigma}^{\mathrm{resp}}
\equiv
\min\{\kappa_{\sigma}^{\mathrm{sel}},\kappa_{\sigma}^{\mathrm{hid}}\}>0.
\]
This is exactly \eqref{eq:combinedcoercive}. All requirements of Definition \ref{def:combined} are satisfied.
\end{proof}

\section{Forced Constituents and Uniqueness}

\begin{proposition}[All combined-response constituents are forced by the selector law]
\label{prop:forced}
Assume Definition \ref{def:generator}. Then the following constituents are fixed by the selector law itself.
\begin{enumerate}
    \item The primitive substrate state is necessarily the faithful quotient of the selector-zero translation space by the inert zero-hidden kernel, represented canonically by
    \[
    \CoreState{\sigma}={\Kernel{\sigma}}^{\perp}\cap\ZeroHiddenVec{\sigma}.
    \]
    \item The primitive substrate body is necessarily the product \(\ProtoBody{\sigma}=\CoreBody{\sigma}\times\GapBody{\sigma}\), where \(\CoreBody{\sigma}\) is the quotient image \eqref{eq:corebody} of the selector-zero body.
    \item The affine observer-data and shell-response components of the combined response map are necessarily the descended selector readouts of \eqref{eq:responsemap} on the faithful zero-hidden quotient, and the hidden response component is necessarily the explicit hidden coordinate in \(\GapSpace{\sigma}\).
    \item The response-image hidden factor is necessarily \(\GapBody{\sigma}\), and the positive hidden-sector operator is necessarily \(\HiddenOperator{\sigma}\).
    \item The descended symmetry / time-reversal structure, the exact zero-response shell realization, fixed-data shell solvability, and the response-kernel coercivity mechanism are all induced by the same selector law and are not additional combined-response freedom.
\end{enumerate}
\end{proposition}

\begin{proof}
Item (1) is exactly the faithful zero-hidden quotient singled out by the selector law: once the selector-zero locus and the inert kernel are fixed, the only faithful Euclidean zero-hidden coordinates are those that quotient out \(\Kernel{\sigma}\), represented canonically by \({\Kernel{\sigma}}^{\perp}\cap\ZeroHiddenVec{\sigma}\).

Item (2) is then forced because the combined response law must retain precisely the selector-zero quotient body together with the already fixed hidden selector body. There is no second compatible hidden factor once \(\GapBody{\sigma}\) is fixed, and there is no second compatible faithful zero-hidden body once the quotient coordinate \eqref{eq:corecoord} is fixed.

Item (3) follows because \(\GenData{\sigma}\) and \(\GenShell{\sigma}\) are affine and the inert kernel lies in the kernels of their linear parts on \(\ZeroHiddenVec{\sigma}\). Therefore those readouts descend uniquely to the faithful quotient, while the hidden response must be exactly the explicit hidden coordinate if the factorization \eqref{eq:responsefactor} is to hold with hidden body \(\GapBody{\sigma}\).

Item (4) is the hidden package already present in Definition \ref{def:generator}. Item (5) is the content of Proposition \ref{prop:combined}: the descended reversible structure, the exact zero-response shell realization, fixed-data solvability, and the coercivity inequality all come directly from the same selector package and carry no extra independent freedom at combined-response level.
\end{proof}

\begin{theorem}[Combined response substrate law necessity / uniqueness from selector law]
\label{thm:necessity}
Assume Definition \ref{def:generator}. Then:
\begin{enumerate}
    \item the canonical construction of Definition \ref{def:canonical} yields a combined response substrate law in the sense of Definition \ref{def:combined};
    \item every combined-response constituent currently left open by the active frontier is forced by the selector law: the faithful primitive substrate state, the primitive substrate body, the affine combined response map, the response-image factorization into shell and hidden sectors, the positive hidden-sector operator, the descended symmetry and time-reversal structure, the exact zero-response shell realization, fixed-data shell solvability, and the response-kernel coercivity mechanism;
    \item any other combined response law compatible with the same selector law differs from the canonical one only by the harmless affine choice of faithful zero-hidden origin and by the obvious orthogonal coordinate identifications on \(\CoreState{\sigma}\) and \(\GapSpace{\sigma}\) preserving the recorded data and shell coordinates;
    \item consequently the combined response substrate law is not an independent remaining freedom once the selector law is fixed.
\end{enumerate}
\end{theorem}

\begin{proof}
Item (1) is Proposition \ref{prop:combined}. Item (2) is Proposition \ref{prop:forced}. For item (3), changing the reference shell point \(\ReferencePoint{\sigma}\) only translates the affine coordinate on the same faithful zero-hidden quotient, while changing orthogonal coordinates on \(\CoreState{\sigma}\) or \(\GapSpace{\sigma}\) only produces the obvious coordinate identifications preserving the same descended selector data, shell readout, and hidden quadratic form. No additional compatible freedom remains. Item (4) is the routing consequence of the previous items.
\end{proof}

\section{Benchmark Preservation}

\begin{theorem}[The derived combined response law preserves the same one-metric benchmark package]
\label{thm:outputs}
Assume Definition \ref{def:generator}. Then the benchmark output statements of Definition \ref{def:fixed} remain unchanged:
\[
\MetricOut(\mathbf w)=\CompletedMetric,
\qquad
\MatterOut(\mathbf w,\psi)
=
T_{\mu\nu}^{\mathrm{matter}}[\CompletedMetric,\psi]
\]
for every \(\mathbf w\in\PrimShell\) and every matter configuration \(\psi\in\Psi\). Consequently the deeper selector law and the induced combined response law introduce neither a second operative metric nor an enlarged low-energy matter law.
\end{theorem}

\begin{proof}
The present note changes only the upstream origin of the combined response package. It does not alter the primitive shell \(\PrimShell\), the recorded metric map \(\MetricOut\), or the recorded matter map \(\MatterOut\). Those remain the fixed continuation data of Definition \ref{def:fixed}, so \eqref{eq:fixedoutputs} continues to hold exactly.
\end{proof}

\section{Main Theorem}

\begin{theorem}[The remaining burden moves below the combined-response layer]
\label{thm:main}
Assume the fixed primitive continuation data of Definition \ref{def:fixed} and, for each sector \(\sigma\in\{\Car,\Cur,\Hom\}\), a zero-hidden selector generator law in the sense of Definition \ref{def:generator}. Then:
\begin{enumerate}
    \item the combined response substrate law of Definition \ref{def:combined} is canonically induced by the selector law after faithful inert-kernel reduction;
    \item the faithful primitive substrate state, the primitive substrate body, the affine combined response map, the response-image factorization into shell and hidden sectors, the positive hidden-sector operator, the descended symmetry and time-reversal structure, the exact zero-response shell realization, fixed-data shell solvability, and the response-kernel coercivity mechanism are all forced by that same selector law;
    \item the one operative metric remains exactly \(\CompletedMetric\), the ordinary matter route remains exactly the standard one through that metric, there is no second operative metric, and the low-energy matter law is not enlarged;
    \item consequently the benchmark-facing burden after the current ambient-dynamics frontier is discharged in the stronger form now required on the active line: the combined response substrate law is no longer an unexplained stopping point, but a canonical and necessary reduction of a deeper zero-hidden selector generator law.
\end{enumerate}
\end{theorem}

\begin{proof}
Item (1) is Theorem \ref{thm:necessity}(1). Item (2) is Theorem \ref{thm:necessity}(2). Item (3) is Theorem \ref{thm:outputs}. Item (4) is the combined routing consequence.
\end{proof}

\begin{corollary}[What remains open after this theorem]
\label{cor:next}
After Theorem \ref{thm:main}, the remaining honest burden is no longer to justify the combined response substrate law itself on the active primitive-observer-reduction line. The next honest burden is deeper again: first try to derive or uniquely force the zero-hidden selector generator law from still more primitive explicit substrate structure in a way that actually changes benchmark-facing status; if that cannot be done cleanly, the admissible fallback is a genuinely smaller theorem-level collapse, sharper necessity result, or sharper uniqueness result strictly inside that selector law while preserving the same benchmark one-metric package and claim boundary.
\end{corollary}

\begin{remark}
Theorem \ref{thm:main} converts the combined response substrate law from the current live burden into a canonical reduction of a deeper selector law. What remains open is why that selector law itself should hold, rather than becoming the next disciplined stopping point. Another ambient-package restatement, trace-core restatement, completion-core restatement, exact-shell affine nucleus restatement, or same-law combined-response relay would therefore leave the current live burden unchanged.
\end{remark}

\section{Conclusion}

The positive result of this note is a direct forcing theorem for the current combined-response stopping point. The previous ambient-dynamics theorem had already shown that the completion-balance ambient package is a canonical reduction of the deeper combined response substrate law. The present note spends the first honest move available there: it shows that the combined response law is itself canonically induced by a deeper zero-hidden selector generator law after quotienting out exactly the selector-zero directions already invisible to the recorded data and shell readouts.

That gain directly addresses the precise constituents that had become the live burden: the primitive substrate state, the primitive substrate body, the affine combined response map, the response-image factorization, the hidden-sector operator, the reversible structure, the exact zero-response shell realization, fixed-data shell solvability, and the response-kernel coercive control. Within the selector-law class those are no longer optional inserted ingredients. They are forced outputs, unique up to the harmless affine choice of faithful zero-hidden origin and the obvious orthogonal identifications on the faithful quotient and hidden factor.

The benchmark boundary remains fixed throughout. The same primitive shell remains the shell carrying the observer outputs, so the one operative metric is still exactly \(\CompletedMetric\), the ordinary matter route is unchanged, there is no second operative metric, and the low-energy matter law is not enlarged. This is therefore a disciplined upstream derivational gain, not a final first-principles completion claim.

The next honest burden is now one level deeper again. The note does not yet derive the zero-hidden selector generator law from ultimate substrate microphysics. The next benchmark-facing attempt should therefore first try to derive or uniquely force that selector law from still more primitive explicit substrate structure in a way that actually changes benchmark-facing status. If that cannot be done cleanly, the admissible fallback is a genuinely smaller theorem-level collapse, sharper necessity result, or sharper uniqueness result strictly inside that selector law. Another ambient-package restatement, trace-core restatement, completion-core restatement, exact-shell affine nucleus restatement, or another same-law combined-response relay that preserves the same selector-law stopping point would not meet that burden, and no stronger first-principles promotion language is licensed yet.

\end{document}
